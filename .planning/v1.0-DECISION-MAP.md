---
version: v1.0
generated: 2026-08-28
status: research-complete
---

# Decision Map — v1.0 milestone

## Phase 1: Tier Legibility

### Gray Areas
- **[tier-field]** Should the new `tier` on `ComponentRegistry.Entry` be a required (no-default) field or a defaulted one? (The enum-on-Entry representation itself is settled — a parallel `Map<name,Tier>` is forbidden by the "entries alone is authoritative" invariant.)
  - **Options:** required (no default) | defaulted
  - **Recommendation:** required (no default), placed last on `Entry` — the Metalava/api.txt rewrite cost is paid either way, so defaulting only surrenders the compile-time "every entry is tiered" guarantee and risks LEG-01 going technically-green but semantically empty
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[gallery-display]** Where and with what component does the gallery surface each component's tier?
  - **Options:** badge on both the list row (`ComponentRow` in `ExplorerIndexScreen`) and the `ComponentDetailScreen` header, reusing Material3 `Badge`/`AssistChip` | reuse the in-house `component/AppChip` | single-surface (detail only)
  - **Recommendation:** badge on both the list row and the detail header (one `ComponentRow` change covers search + all per-family lists), reusing an existing chip/badge rather than a new primitive — single-surface fails "a developer browsing the catalog can see tiering without reading source"
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[litmus]** How is the per-tier litmus in `docs/DESIGN-INTENT.md` phrased, and how are borderline components (e.g. `CardBase`, `ChipBar`, `HeatSwatch`) tiered? (That the doc is new and distinct from `API.md` is settled by LEG-02.)
  - **Options:** anchor the litmus to the existing one-way-dependency / no-domain-assumption invariant (primitive = zero domain nouns in name+params, renders only caller-passed content; pattern = encodes an opinion/composition/interaction convention) — a decidable test | adjective-based ("simpler" vs "more opinionated")
  - **Recommendation:** anchor to the invariant so the litmus is a decidable test — an adjective-based litmus lets the same component be tiered two ways, making Phase 2's altitude-mismatch findings un-defensible and Phase 3's tier-aware gate unwritable
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —

## Phase 2: Coherence Audit

### Gray Areas
- **[tier-source]** Does the audit consume Phase 1's ratified per-entry `tier` labels + the `DESIGN-INTENT.md` litmus to name "altitude mismatch," or derive tier judgements itself?
  - **Options:** consume Phase 1's ratified `entries[i].tier` labels | re-derive tier ad hoc by applying the litmus per component
  - **Recommendation:** consume Phase 1's ratified labels — re-deriving rests findings on an unratified taxonomy that can contradict the shipped gallery badge and makes the finding unfalsifiable
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** yes
  - **Depends-on:** 1
- **[blast-radius]** For each "unify" finding, does the audit pre-compute consumer blast radius by read-only grep of the two on-disk consumer repos, or stay hub-only and defer call-site discovery to Phase 5?
  - **Options:** read-only grep both consumer repos (`~/Projects/SecondBrain`, `~/Projects/CalTracker_Android`) now, recording call-sites per unify | hub-only — list the hub symbols each unify removes, defer call-site discovery to Phase 5
  - **Recommendation:** read-only grep both consumer repos now (read-for-blast-radius is sanctioned; only *editing* consumer files violates sequential-in-hub) — it gives Phase 5 a pre-flight against the documented stranded-consumer failure mode
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —

## Phase 3: Governance Gates

### Gray Areas
- **[gov03-fix]** GOV-03 root cause is reproduced — the pre-commit hook classifies the *whole working tree against the last release tag*, so a legitimate post-tag src edit (`HeatSwatch.kt` comment fix in `5b01532`) is inherited by every later commit, including pure `.planning/` ones. Which fix mechanism?
  - **Options:** classify the commit's own staged delta (`git diff --cached` vs `HEAD`, ∩ `src/main`) | keep the tag-baseline model but restrict `PATHS` to staged files ∩ `src/main`
  - **Recommendation:** staged-delta diff (`--cached` vs `HEAD`) — cleanest, re-flags only the introducing commit; the tag-baseline-restricted alternative still sees prior same-file rewrites
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[gov02-semantics]** What does "flag not forbid" mean concretely for the GOV-02 domain-noun guard?
  - **Options:** fail-until-allowlisted JUnit test (forbid-until-acknowledged; cleared by adding the name + rationale to an `INTENTIONALLY_UNREGISTERED`-style allowlist) | non-blocking always-green report emitted to a file/CI log
  - **Recommendation:** fail-until-allowlisted (audit-trailed, CI-enforced) — an always-green warning is ignorable and defeats "surface the coupling for review"; a hard permanent forbid red-builds day one on the ~10 grandfathered domain names
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[gov02-detection]** How does the guard detect a "domain noun" *without importing domain vocabulary* (which would itself breach the one-way / no-domain-assumption invariant)?
  - **Options:** inverse allowlist of the hub's own UI-primitive nouns (`Card`, `Chip`, `Sheet`, `Button`, `Bar`, `Swatch`, `Picker`…) — flag any net-new name whose head token falls outside it | token-shape heuristic (flag any capitalized noun token that is not a known Compose/Material type)
  - **Recommendation:** inverse allowlist of hub UI-primitive nouns — structural, ships no consumer terms in the hub; a consumer-term denylist would import the domain knowledge the guard exists to prevent
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[litmus-location]** Where does the tier-aware contribution litmus live — extend Phase 1's `docs/DESIGN-INTENT.md`, or a new `CONTRIBUTING.md`? ("Enforced where feasible" resolves to the GOV-02 guard checking the strict-primitives half; the patterns-loose half stays prose.)
  - **Options:** extend `docs/DESIGN-INTENT.md` with a contribution-litmus section | new `CONTRIBUTING.md` cross-linking `DESIGN-INTENT.md`
  - **Recommendation:** extend `docs/DESIGN-INTENT.md` (P1 already commits to documenting the per-tier litmus there) — a separate `CONTRIBUTING.md` risks double-sourcing the tier prose; no `.github/` PR-template or CI-review surface exists to wire a checklist into
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** yes
  - **Depends-on:** 1

## Phase 4: Repin Bookkeeping Hardening

### Gray Areas
- **[consumer-name]** `reconcile` renders CalTracker's row from its checkout dir name (`CalTracker_Android`), not `CalTracker`. How is the seeded row named so the first run is idempotent? (That the matrix is a *separate* machine-owned `<!-- repin-matrix -->` block — never wrapping the §1 human prose table `reconcile` would overwrite — is settled.)
  - **Options:** seed the row name as `CalTracker_Android` (matches render exactly → byte-stable first run) | seed `CalTracker` and accept the first `reconcile` rewriting it (fuzzy prefix-match tolerates the mismatch on `validate`)
  - **Recommendation:** seed `CalTracker_Android` — `render_matrix_block` always writes the dir name, so seeding `CalTracker` makes run-one report a one-time rename that a byte-stability verifier could misread as a failure
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[inc-closure]** How is `INC-2026-08-28-03` closure (success criterion 3) demonstrated — the incident lives in the control-plane log, not this repo, so its worded acceptance condition isn't verified against the reconcile evidence yet.
  - **Options:** confirm the incident's worded acceptance text via the `incident` tooling / control-plane log, then map reconcile evidence 1:1 to it before marking closed | treat a clean `reconcile` run (no hand edits, true pins for both consumers) as sufficient closure
  - **Recommendation:** confirm the incident text via the control-plane `incident` tooling first, so the reconcile evidence maps 1:1 to what the incident actually asks closed
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —

## Phase 5: Gardening — Unify & Coordinated Repin

### Gray Areas
- **[unify-scope]** What is the concrete unify work-order? (This is Phase 5's defining provisionality — the list is Phase 2's `docs/COHERENCE-AUDIT.md` "unify" dispositions, which don't exist until Phase 2 runs.)
  - **Options:** implement exactly Phase 2's dispositioned unify tuples 1:1, adding/removing no scope at execution time | re-analyze overlaps at execution and set scope in Phase 5
  - **Recommendation:** implement exactly Phase 2's unify tuples 1:1 — re-analyzing desyncs the unify count from the audit and breaks the 1:1 mapping the api.txt rebaseline diff review depends on
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** yes
  - **Depends-on:** 2
- **[fold-mechanism]** Per fold, is the removed sibling deleted from its family list entirely, or demoted into `INTENTIONALLY_UNREGISTERED` with a one-line reason? (The general mechanic — update the registry cell, `./gradlew apiDump` rebaseline with line-by-line review, commit via curation lane `HUB_LANE_OVERRIDE=3` — is settled.)
  - **Options:** remove entirely | demote to `INTENTIONALLY_UNREGISTERED` with a rationale
  - **Recommendation:** decide per fold from Phase 2's disposition wording — default to remove for a `prune`, fold-then-demote (or remove) for a `unify`; keeping registered-XOR-allowlisted true either way
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** yes
  - **Depends-on:** 2
- **[semver]** The gardening tag is the first true break in an all-additive `v1.0.0→v1.10.0` history. What version number?
  - **Options:** v2.0.0 (semver-correct major) | continue the v1.x minor cadence (e.g. v1.11.0)
  - **Recommendation:** v2.0.0 — a break under a non-breaking version number semver-lies to consumers and any future range-based resolution would silently pull the break; the tag-cut itself stays human-gated regardless
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[repin-seam]** How do the two consumer repins execute given sequential-in-hub (which forbids editing consumer files from a hub task)?
  - **Options:** the hub cuts the tag on `main` here; each consumer repin runs through that consumer's own channel (human-gated), staged so the ecosystem isn't "moved" until both are ready + Gate-1 re-verified | execute both consumer repins directly from this Phase 5 hub task
  - **Recommendation:** hub cuts the tag only; consumer repins run via each consumer's own channel as the human-gated cross-repo exception, with `repin_status.py reconcile` (Phase 4) proving both pins moved — editing consumer files from the hub phase violates sequential-in-hub
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
- **[caltracker-catchup]** CalTracker sits at `v1.5.0` (7 additive tags behind); moving it straight to the gardening tag crosses all of `v1.6.0→v1.10.0` plus the break in one jump. Does it need an intermediate catch-up hop first?
  - **Options:** intermediate catch-up repin to `v1.10.0` (Gate-1 verified) before moving to the gardening tag | single jump straight to the gardening tag
  - **Recommendation:** intermediate catch-up hop first — a single jump gives CalTracker a far wider blast radius than SecondBrain's single-tag move, raising the odds an unrelated regression surfaces at its Gate-1 and strands it while SecondBrain passes
  - **Resolution:** _unresolved_
  - **Source:** research-default
  - **Provisional:** no
  - **Depends-on:** —
