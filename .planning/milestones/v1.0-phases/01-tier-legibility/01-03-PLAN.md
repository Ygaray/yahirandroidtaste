---
phase: 01-tier-legibility
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt
autonomous: true
requirements: [LEG-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 18 Sheets-family entries carry an explicit tier value applying the D-03 litmus (domain noun in name+params OR baked-in interaction/composition convention => PATTERN; neither => PRIMITIVE)."
    - "SheetScaffold and ClearableTextField are tiered PRIMITIVE (generic chrome/generic text field, zero domain nouns, render only caller-passed content) — the two genuine Sheets-family primitives among otherwise domain-flavored modal sheets."
  artifacts:
    - "SheetsFamilyScreen.kt — all 18 Entry(...) call sites carry tier = ..."
  key_links:
    - "SheetsFamilyScreen.kt's 18 tiered entries -> ComponentRegistry.entries (unchanged concatenation order) -> full-project compile, which succeeds only once this plan AND 01-04-PLAN.md have both landed (Wave 3, 01-05-PLAN.md, is the first point that check can run)."
---

<objective>
Apply the D-03 litmus to tier all 18 Sheets-family entries in `SheetsFamilyScreen.kt` — the
largest single family in the registry (18 of 53 entries), continuing the tiering sweep started in
`01-01-PLAN.md`.

Purpose: `Entry.tier` (added in `01-01-PLAN.md`) is required with no default, so this file's 18
`ComponentRegistry.Entry(...)` construction sites must ALL supply `tier = ...` before the module
compiles again. This plan depends on `01-01-PLAN.md` (Wave 1) for the `ComponentRegistry.Tier`
enum to exist. It runs in Wave 2 in parallel with `01-04-PLAN.md` (the remaining 6 families) — the
two plans touch disjoint files, so they can execute concurrently, but NEITHER can reach a full
green project compile on its own (each is missing the other's sites); that full compile is deferred
to `01-05-PLAN.md` (Wave 3), the first point after both merge. This plan's own verification is
therefore source-level (grep-confirmed per batch), matching this file's own existing batch
structure (its Variants-wrapper section is already commented "Task 1 (batch A)" / "Task 2 (batch
B)" / "Task 3 (batch C)") — this plan reuses that exact grouping.

Output: 18 of 53 registry entries (Sheets family) tiered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-tier-legibility/01-CONTEXT.md
@.planning/phases/01-tier-legibility/01-RESEARCH.md
@.planning/phases/01-tier-legibility/01-01-PLAN.md
</context>

## Artifacts this phase produces

- `ComponentRegistry.Tier` — new nested enum (`PRIMITIVE`, `PATTERN`) in `ComponentRegistry.kt`
  (landed in `01-01-PLAN.md`)
- `ComponentRegistry.Entry.tier` — new required field on `Entry` (landed in `01-01-PLAN.md`)
- `ComponentRow`'s new `tier: ComponentRegistry.Tier` parameter — `ExplorerIndexScreen.kt` (lands
  in `01-05-PLAN.md`)
- New `Badge` render block inside `ComponentDetailScreen`'s `TopAppBar` title (lands in
  `01-05-PLAN.md`)
- `docs/DESIGN-INTENT.md` — new file (landed in `01-02-PLAN.md`)
- `ComponentRegistryTierTest.kt` — new test file (lands in `01-05-PLAN.md`)
- `api.txt` — regenerated (lands in `01-05-PLAN.md`)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tier batch A — 6 entries (AlbumSourcePickerSheet, AlbumTitleConfirmSheet, ListCardBottomSheet, TagPickerSheetContent, TagPickerSheet, TextCardBottomSheet)</name>
  <precondition>ComponentRegistry.Tier enum exists in ComponentRegistry.kt (landed by 01-01-PLAN.md, Wave 1)</precondition>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt</files>
  <action>
  Sheets entries use POSITIONAL `name`/`family` args (e.g. `ComponentRegistry.Entry("AlbumSourcePickerSheet", ExplorerFamilies.SHEETS, states = ..., content = ...)`), so `tier = ComponentRegistry.Tier.X` must be added as a NAMED trailing argument (Kotlin requires all args after the first named one to stay named — `content =` already is). Add it to these 6 entries, applying the D-03 litmus:

  - AlbumSourcePickerSheet -> PATTERN ("Album" domain noun + modal source-picker chrome convention)
  - AlbumTitleConfirmSheet -> PATTERN ("Album" domain noun + modal confirm-dialog convention)
  - ListCardBottomSheet -> PATTERN ("List"+"Card" domain nouns)
  - TagPickerSheetContent -> PATTERN ("Tag" domain noun, embeds picker business logic)
  - TagPickerSheet -> PATTERN ("Tag" domain noun, modal wrapper around TagPickerSheetContent)
  - TextCardBottomSheet -> PATTERN ("Text"+"Card" domain nouns)

  Use fully-qualified `ComponentRegistry.Tier.PATTERN` (matches this file's existing
  fully-qualified `ComponentRegistry.Entry(...)` style — no new import needed).
  </action>
  <verify>
    <automated>grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.' | grep -qx 6 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'` returns 6
  - `grep -c 'tier = ComponentRegistry.Tier.PRIMITIVE' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt` returns 0 (all 6 in batch A are PATTERN)
  </acceptance_criteria>
  <done>6 of 18 Sheets entries (batch A) carry an explicit tier value.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tier batch B — 7 entries (TagChipEditorContent, CardEditorShellContent, RecordingBottomSheetContent, SheetScaffold, BulkCreatePopup, BulkCreatePopupContent, NameAndTagsEditor)</name>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt</files>
  <action>
  Add `tier = ComponentRegistry.Tier.X` as a named trailing argument to these 7 entries, applying
  the D-03 litmus:

  - TagChipEditorContent -> PATTERN ("Tag" domain noun + baked-in last-tag-warning business logic)
  - CardEditorShellContent -> PATTERN ("Card" domain noun, full editor shell composition)
  - RecordingBottomSheetContent -> PATTERN ("Recording"/voice domain noun + modal sheet convention)
  - SheetScaffold -> PRIMITIVE (`SheetScaffold(onDismissRequest, content: @Composable () -&gt; Unit)`
    — zero domain nouns, generic chrome-only wrapper rendering only caller-passed `content`)
  - BulkCreatePopup -> PATTERN (bakes in the "create and more" secondary-action interaction
    convention, not neutral caller content)
  - BulkCreatePopupContent -> PATTERN (same convention as BulkCreatePopup, its body)
  - NameAndTagsEditor -> PATTERN ("Tags" domain noun + composed name+tags editor convention)

  Use fully-qualified `ComponentRegistry.Tier.X`.
  </action>
  <verify>
    <automated>grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.' | grep -qx 13 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'` returns 13 (cumulative: 6 from batch A + 7 from batch B)
  - `grep -A2 'name = "SheetScaffold"\|"SheetScaffold",' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt` region contains `Tier.PRIMITIVE`
  </acceptance_criteria>
  <done>13 of 18 Sheets entries (batches A+B) carry an explicit tier value.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tier batch C — 5 entries (TagCreateSheet, TagCreateSheetContent, VoiceRenameTagsSheet, ClearableTextField, EditorItemRow)</name>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt</files>
  <action>
  Add `tier = ComponentRegistry.Tier.X` as a named trailing argument to these final 5 entries,
  applying the D-03 litmus — this completes all 18 Sheets entries:

  - TagCreateSheet -> PATTERN ("Tag" domain noun, modal creation sheet)
  - TagCreateSheetContent -> PATTERN ("Tag" domain noun, its body)
  - VoiceRenameTagsSheet -> PATTERN ("Voice"+"Tags" domain nouns)
  - ClearableTextField -> PRIMITIVE (`ClearableTextField(value: String, onValueChange, label,
    modifier)` — zero domain nouns, generic reusable text field with a clear-`x` affordance,
    renders only caller-passed content)
  - EditorItemRow -> PATTERN (bakes in a specific reorderable-list-row interaction convention:
    drag handle, checkbox, `subType` string, not neutral caller content)

  Use fully-qualified `ComponentRegistry.Tier.X`.
  </action>
  <verify>
    <automated>grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.' | grep -qx 18 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'` returns 18 (all Sheets entries tiered)
  - `grep -c 'tier = ComponentRegistry.Tier.PRIMITIVE' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt` returns exactly 2 (SheetScaffold, ClearableTextField)
  - `grep -c 'ComponentRegistry.Entry(' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt` equals 18 (sanity: entry count unchanged)
  </acceptance_criteria>
  <done>All 18 Sheets entries carry an explicit tier value. Combined with 01-01-PLAN.md (16) and pending 01-04-PLAN.md (19), 34 of 53 registry entries are tiered once this plan completes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan populates a compile-time enum field at 18 source-code call sites. No user input, no network I/O, no auth/session, no persisted/serialized data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-01 | Tampering | `SheetsFamilyScreen.kt` `Entry.tier` assignments | low | accept | `Tier` is a closed 2-value Kotlin enum, assigned only by source code under repo-owner control at compile time — no runtime/user-supplied string is parsed into it. |

No package installs this phase (Package Legitimacy Gate not applicable, per RESEARCH.md).
</threat_model>

<verification>
- All 18 Sheets `ComponentRegistry.Entry(...)` call sites carry `tier = ComponentRegistry.Tier.X`
- Exactly 2 are PRIMITIVE (SheetScaffold, ClearableTextField); the other 16 are PATTERN
- Entry count in the file is unchanged (18) — no entries added, removed, or duplicated
- Full project compile is NOT expected to pass yet (ButtonsFab/Pickers/Feedback/EmptyState/
  Progress/TactileFoundation still untiered until 01-04-PLAN.md lands) — resolved by 01-05-PLAN.md
</verification>

<success_criteria>
All 18 Sheets-family entries carry a defensible tier value per the D-03 litmus.
</success_criteria>

<output>
Create `.planning/phases/01-tier-legibility/01-03-SUMMARY.md` when done
</output>
