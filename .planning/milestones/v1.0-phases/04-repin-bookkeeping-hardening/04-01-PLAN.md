---
phase: 04-repin-bookkeeping-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ECOSYSTEM.md
  - /home/yahir/Projects/yahir-agentic-tools/yahir-gsd-control-plane/incidents/INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md
  - /home/yahir/Projects/yahir-agentic-tools/yahir-gsd-control-plane/incidents/index.json
autonomous: true
requirements: [REPIN-01]

estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ECOSYSTEM.md carries a machine-owned <!-- repin-matrix:begin/end --> block, separate from the §1 human prose table (D-01)"
    - "repin_status.py reconcile --hub yahirandroidtaste exits 0 without ValueError and requires no hand edits to ECOSYSTEM.md"
    - "A second consecutive reconcile run reports no drift, proving idempotency (SC2)"
    - "The ECOSYSTEM.md change is committed to the hub repo at Lane 1, with no HUB_LANE_OVERRIDE needed"
    - "INC-2026-08-28-03 is closed (status: closed, resolution: fixed) with evidence 1:1-mapped to its worded acceptance text (D-02, SC3)"
  artifacts:
    - "ECOSYSTEM.md (repin-matrix block inserted and committed)"
    - "yahir-gsd-control-plane/incidents/INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md (status: closed)"
    - "yahir-gsd-control-plane/incidents/index.json (regenerated)"
  key_links:
    - "ECOSYSTEM.md's <!-- repin-matrix --> block <-> repin_status.py's parse_ecosystem_matrix()/_matrix_region() parser contract"
    - "validate/reconcile CLI evidence <-> INC-2026-08-28-03's Proposed-fix acceptance test + ROADMAP SC3 wording"
---

<objective>
Make `ECOSYSTEM.md` carry the machine-owned `repin-matrix` markers so `repin_status.py reconcile`
operates on this hub without hand edits, then close `INC-2026-08-28-03` with evidence mapped 1:1
to its acceptance text.

Purpose: closes the standing incident and proves — live, not by inference — that repin
reconciliation for both consumers (SecondBrain, CalTracker_Android) is now a tooling operation
ahead of Phase 5's coordinated repin, which depends on this bookkeeping being trustworthy.
Output: a committed `ECOSYSTEM.md` carrying the `<!-- repin-matrix:begin/end -->` block (D-01),
and `INC-2026-08-28-03` closed in the control-plane repo (D-02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-repin-bookkeeping-hardening/04-CONTEXT.md
@.planning/phases/04-repin-bookkeeping-hardening/04-RESEARCH.md
@ECOSYSTEM.md
@$HOME/.claude/skills/incident/SKILL.md
@/home/yahir/Projects/yahir-agentic-tools/yahir-gsd-control-plane/incidents/INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md
@/home/yahir/Projects/yahir-agentic-tools/yahir-gsd-control-plane/incidents/_schema.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Seed the repin-matrix block in ECOSYSTEM.md and prove reconcile works end-to-end</name>
  <precondition>git ls-remote --tags https://github.com/Ygaray/yahirandroidtaste succeeds (network reachable) — repin_status.py's reconcile fails closed with ValueError if any consumer's tag status comes back unknown/offline (RESEARCH.md Pitfall 2); a 1h local cache at ~/.cache/repin-status/tags.json may already satisfy this from a recent fetch.</precondition>
  <files>ECOSYSTEM.md</files>
  <action>
Re-derive current truth live rather than reusing RESEARCH.md's snapshot numbers (time may have
passed since research, per RESEARCH.md's own Anti-Pattern warning): run
`python3 ~/.claude/context/deps/repin_status.py status --hub yahirandroidtaste --json` and note
the live consumer/pin/latest/status values it reports.

Read ECOSYSTEM.md and locate the anchor confirmed by RESEARCH.md: the existing §1 closing
parenthetical note (the sentence beginning "Best-effort cache — keep it current" and ending
"No "Deploy host" column — Android apps are installed on devices, not daemon-deployed.)_"),
immediately followed by a blank line and then the "**Current published tag:**" paragraph. Insert
a new subsection directly after that closing note and before the "**Current published tag:**"
paragraph, as a sibling of the existing §1 human-prose consumer table — never wrapping or
replacing that table (D-01, RESEARCH.md's single highest-consequence anti-pattern to avoid, since
`reconcile` overwrites everything strictly between the markers on every future run). The new
subsection is: a `### Machine-reconciled pin matrix` heading; a blockquote note stating it is
regenerated by `repin_status.py reconcile --hub yahirandroidtaste` (see
`~/.claude/context/workflows/repin.md`) and that the rows between the markers must not be
hand-edited; then the `<!-- repin-matrix:begin -->` / `<!-- repin-matrix:end -->` marker pair
wrapping ONLY the 4-column table `render_matrix_block()` emits — no narrative text inside the
fence (both markers are required — a single marker still raises ValueError, RESEARCH.md
Pitfall 1).

Do not hand-type the table rows. Compute the exact block by importing `repin_status.py` from
`~/.claude/context/deps` in a live Python session and calling
`render_matrix_block(status_report(hub="yahirandroidtaste"))`, then paste that literal output
between the markers verbatim. This automatically seeds the CalTracker row using the
checkout-directory-derived name `CalTracker_Android` (D-01) — `_consumer_name()` always derives
the row name from the checkout dir under `~/Projects`, so do not override it or type `CalTracker`
instead; doing so would make the very first `reconcile` run rename the row, which a byte-stability
check could misread as a failure.
  </action>
  <verify>
    <automated>python3 ~/.claude/context/deps/repin_status.py validate --hub yahirandroidtaste && python3 ~/.claude/context/deps/repin_status.py reconcile --hub yahirandroidtaste && python3 ~/.claude/context/deps/repin_status.py reconcile --hub yahirandroidtaste</automated>
  </verify>
  <done>
`validate` prints "yahirandroidtaste: ECOSYSTEM.md matrix matches truth" and exits 0. Both
`reconcile` runs exit 0 with no `ValueError` — either both report "no drift" (correctly-seeded
first run, D-01's intent), or the first reports a real write (if live tag state moved since the
status check) followed by "no drift" on the second run (idempotency, RESEARCH.md Pitfall 3). No
hand edits to ECOSYSTEM.md were needed beyond this task's own insertion.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit the ECOSYSTEM.md change, confirming the pre-commit lane-gate stays clean</name>
  <files>ECOSYSTEM.md</files>
  <action>
Stage the change (`git add ECOSYSTEM.md`) and first confirm Lane 1 still holds for this exact
staged diff by running the real dry-run classifier directly (not just trusting RESEARCH.md's
earlier live test):
`API_FILE=$(git rev-parse --show-toplevel)/api.txt bash tools/classify-hub-change.sh --baseline $(git describe --tags --abbrev=0 --match 'v*')`
— expect `LANE 1`, exit 0 (RESEARCH.md Pitfall 4: GOV-03 already fixed the false-flag this repo's
own stale session memory describes for `.planning/`/doc-only commits; do not add
`HUB_LANE_OVERRIDE` preemptively).

Then run the real commit via `git commit` (not the dry-run alone), letting the repo's actual
`.git/hooks/pre-commit` symlink (→ `tools/hooks/pre-commit`) invoke `tools/classify-hub-change.sh`
itself — it already exports `API_FILE` internally, so no manual export or `HUB_LANE_OVERRIDE` is
needed for a real commit either. Write the commit message referencing both `REPIN-01` and
`INC-2026-08-28-03` (e.g. a `docs(04):` conventional-commit subject naming both explicitly). If
the real `git commit` unexpectedly triggers a lane-2 classification despite the dry run reporting
Lane 1, stop and surface the discrepancy rather than silently adding `HUB_LANE_OVERRIDE` — that
would contradict this phase's "no hand edits/overrides needed" success criterion.
  </action>
  <verify>
    <automated>git log -1 --pretty=%B | grep -q "REPIN-01" && git diff --quiet -- ECOSYSTEM.md</automated>
  </verify>
  <done>
The pre-commit dry-run reports LANE 1 for the staged diff, the real `git commit` lands cleanly
with no `HUB_LANE_OVERRIDE`, the commit message references REPIN-01 and INC-2026-08-28-03, and
`ECOSYSTEM.md` has no remaining uncommitted diff.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Close INC-2026-08-28-03 with evidence mapped 1:1 to its acceptance text</name>
  <files>/home/yahir/Projects/yahir-agentic-tools/yahir-gsd-control-plane/incidents/INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md, /home/yahir/Projects/yahir-agentic-tools/yahir-gsd-control-plane/incidents/index.json</files>
  <action>
Per D-02, confirm the incident's own worded acceptance text before touching its status — re-read
`INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md`'s
current frontmatter (`status: open`, `resolution: null`) and its "Proposed fix" section's item 3
acceptance test verbatim. Then drive the `incident` skill's lifecycle against this exact incident:

`diagnose INC-2026-08-28-03` — record the root cause with the evidence that confirms it (cite
Task 1's live-verified insertion and the reproduce-the-failure command from RESEARCH.md's Code
Examples showing the pre-fix `ValueError`), set `status: diagnosed`.

`fix INC-2026-08-28-03` — append a Resolution section citing `ECOSYSTEM.md` plus Task 2's commit
sha, set `status: fixed` ("fixed" means applied, not yet proven, per the skill's own contract).

`verify INC-2026-08-28-03` — dispatch the `incident-verifier` subagent per the skill's documented
invocation, passing it Task 1's exact verification commands (validate + reconcile x2) so it
independently re-runs them against the now-committed real `ECOSYSTEM.md` rather than trusting
pasted text (RESEARCH.md Open Question 2). On CONFIRMED, set `status: verified` and
`verified_by`. On REFUTED or UNPROVEN, stop and surface rather than proceeding to close.

`close INC-2026-08-28-03 --resolution fixed` — map the pasted evidence 1:1 to the incident's own
"Proposed fix" acceptance-test wording AND to ROADMAP.md SC3's less literal phrasing ("the
reconcile output reflects the true pin state for both consumers"). A `no drift` result on
correctly-seeded data is the expected, correct pass condition per RESEARCH.md Pitfall 3 — not a
failure to demonstrate a "real" write; do not misread the incident's literal "updates the matrix
rows" wording as requiring a visible diff when the seeded values already match live truth.

Regenerate the index from the control-plane repo root
(`python3 bin/reindex-incidents.py`) and commit the incident `.md` and `incidents/index.json`
together in that repo (never hand-edit `index.json`).
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/yahir-agentic-tools/yahir-gsd-control-plane && grep -q "status: closed" incidents/INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md && grep -q "resolution: fixed" incidents/INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md && git diff --quiet -- incidents/INC-2026-08-28-03-hub-ecosystem-md-missing-repin-matrix-markers-reconcile-valueerror.md incidents/index.json</automated>
  </verify>
  <done>
`INC-2026-08-28-03`'s frontmatter reads `status: closed`, `resolution: fixed`, `verified_by` set
to the `incident-verifier`'s CONFIRMED result; `index.json` regenerated; both files committed in
the control-plane repo with evidence 1:1-mapped to the incident's acceptance text and to ROADMAP
SC3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase edits a public Markdown document (`ECOSYSTEM.md`, already public in this open-source repo) and a local-machine incident-tracking record in a separate local repo. It introduces no authentication, session, network-input-parsing-from-untrusted-sources, or cryptographic surface (RESEARCH.md § Security Domain — ASVS review included for completeness per `security_enforcement: true`, no applicable category found). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `ECOSYSTEM.md` `<!-- repin-matrix -->` block | low | accept | The seeded table is agent-authored directly from `repin_status.py`'s own live `render_matrix_block()` output, not attacker-controlled input; the file is public and version-controlled, so any bad edit is visible in `git diff`/history and trivially revertible. |

</threat_model>

<verification>
Run the full sequence in order: Task 1's `validate` + `reconcile` (x2) against the seeded
`ECOSYSTEM.md`; Task 2's pre-commit dry-run + real `git commit`; Task 3's incident lifecycle
through `close --resolution fixed`. All three must be green before this phase is considered
complete — this maps directly to ROADMAP.md's three success criteria (markers present, reconcile
succeeds with no hand edits, incident verifiably closed with reconcile output reflecting true pin
state for both consumers).
</verification>

<success_criteria>
1. `ECOSYSTEM.md` carries the `<!-- repin-matrix:begin/end -->` marker pair, seeded from live
   `render_matrix_block()` output, committed without `HUB_LANE_OVERRIDE`.
2. `repin_status.py reconcile --hub yahirandroidtaste` succeeds twice in a row (idempotent) with
   no hand edits to `ECOSYSTEM.md`.
3. `INC-2026-08-28-03` is `status: closed`, `resolution: fixed`, with evidence mapped 1:1 to its
   acceptance text and to ROADMAP SC3's wording.
</success_criteria>

<output>
Create `.planning/phases/04-repin-bookkeeping-hardening/04-01-SUMMARY.md` when done
</output>
