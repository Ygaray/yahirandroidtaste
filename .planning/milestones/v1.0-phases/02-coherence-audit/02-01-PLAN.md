---
phase: 02-coherence-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/COHERENCE-AUDIT.md
autonomous: true
requirements: [AUD-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A developer reading docs/COHERENCE-AUDIT.md sees the Cards (11), Chips (5), Sheets (18), and Tactile Foundation (4) families fully enumerated by name + shipped tier, none re-derived."
    - "Every overlap/near-duplicate-sibling/altitude-mismatch finding raised in these 4 families carries an explicit unify/keep-with-rationale/prune disposition with cited rationale (D-03 litmus or DESIGN-INTENT.md worked-example pattern)."
    - "Every 'unify' finding in these 4 families carries pre-computed SecondBrain + CalTracker_Android call-site counts (D-02)."
  artifacts:
    - "docs/COHERENCE-AUDIT.md"
  key_links:
    - "COHERENCE-AUDIT.md Cards/Chips/Sheets/Tactile-Foundation sections -> shipped Entry.tier values in CardsFamilyScreen.kt / ChipsFamilyScreen.kt / SheetsFamilyScreen.kt / TactileFoundationFamilyScreen.kt (must not contradict, per D-01)"
---

<objective>
Create `docs/COHERENCE-AUDIT.md` with its full 9-family + Unify-Work-Order skeleton (tracer), then
write the real, dispositioned audit content for the four families with the strongest seed
material identified in RESEARCH.md: Cards, Chips, Sheets, Tactile Foundation.

Purpose: AUD-01 requires every registered family enumerated and every overlap/near-duplicate/
altitude finding dispositioned. This plan proves the document's end-to-end shape (tracer) and
then knocks out 38 of the 53 registry entries — the four families RESEARCH.md's skim already
surfaced concrete candidates in — leaving the remaining 15 lower-signal entries plus final
assembly to 02-02-PLAN.md (wave 2, same file, sequenced after this plan).
Output: `docs/COHERENCE-AUDIT.md` with 10 `### ` headings (9 families + Unify Work-Order), the
Cards/Chips/Sheets/Tactile-Foundation sections fully written, the remaining 6 sections still
carrying the tracer's placeholder text.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-coherence-audit/02-CONTEXT.md
@.planning/phases/02-coherence-audit/02-RESEARCH.md
@docs/DESIGN-INTENT.md

# Source files this plan reads (never re-derive tier — transcribe Entry.tier verbatim, D-01)
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistry.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/CardsFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create docs/COHERENCE-AUDIT.md skeleton — all 9 families + Unify Work-Order, structurally verified</name>
  <files>docs/COHERENCE-AUDIT.md</files>
  <action>
Create `docs/COHERENCE-AUDIT.md`. Open with a one-paragraph intro stating the audit's purpose
(surface and disposition overlap/near-duplicate-sibling/altitude-mismatch findings across the 9
registered families) and a short "Scope & Method" subsection that: (a) states tier is consumed
from Phase 1's ratified `Entry.tier` values and never re-derived (D-01, cite `docs/DESIGN-INTENT.md`
for the D-03 litmus rather than re-pasting it), and (b) states that every "unify" finding carries a
read-only consumer blast-radius grep against `~/Projects/SecondBrain` and `~/Projects/CalTracker_Android`
(D-02), noting the grep is a single-name lower bound (a consumer could alias an import). Also
note: CalTracker_Android is pinned to hub tag `v1.5.0` while SecondBrain is pinned to `v1.10.0`
(older on-disk snapshot) — when a "unify" finding's blast-radius grep returns zero CalTracker
hits, every later task should check that component's family-screen file for a phase-provenance
comment (e.g. Tactile Foundation entries carry "Phase 123"-style notes) and record whether the
component simply postdates CalTracker's pin, so a zero-hit count reads correctly as "too new for
that pin" rather than "genuinely unused."

Then add exactly 9 `### ` family headings, in `ExplorerFamilies.ORDERED_KEYS` order/labels
(`ExplorerIndexScreen.kt`): `### Cards`, `### Chips`, `### Sheets`, `### Buttons / FAB`,
`### Pickers`, `### Feedback`, `### Empty State`, `### Progress / Metrics`,
`### Tactile Foundation`. Under each, insert the exact placeholder line
`_(PENDING - filled by a later task)_` and nothing else.

Finally add one `### Unify Work-Order` heading with the same exact placeholder line
`_(PENDING - filled by a later task)_` underneath — this is the section Phase 5 (Gardening)
consumes 1:1; it stays a placeholder until every family section above it is dispositioned.

Do not write any real findings in this task — the goal is the exact 10-heading skeleton, proven
structurally, before content is added.
  </action>
  <verify>
    <automated>for f in Cards Chips Sheets "Buttons / FAB" Pickers Feedback "Empty State" "Progress / Metrics" "Tactile Foundation" "Unify Work-Order"; do grep -q "^### $f\$" docs/COHERENCE-AUDIT.md || echo "MISSING: $f"; done</automated>
  </verify>
  <done>docs/COHERENCE-AUDIT.md exists with exactly 10 `### ` headings (9 families in ExplorerFamilies.ORDERED_KEYS order + Unify Work-Order), the structural grep above prints nothing, and `grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md` returns 10. File committed.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Cards + Chips families — enumerate, disposition, blast-radius</name>
  <files>docs/COHERENCE-AUDIT.md</files>
  <action>
Replace the `### Cards` placeholder: transcribe all 11 entries' `name` + shipped `tier` verbatim
from `CardsFamilyScreen.kt`'s `cardsFamilyEntries` list (never re-derive, D-01) into a table or
list. Then write up the near-duplicate-sibling candidate: `Read` `TextCard.kt`, `ListCard.kt`,
`AlbumCard.kt`, `VoiceCard.kt`, and `CardQuickView.kt`. Confirm (already verified this session)
that `TextCard`/`ListCard`/`AlbumCard`/`VoiceCard` each directly compose `CardBase(...)` and each
expose an identically-shaped `isPinned` `Control.Toggle` in their registry `Entry` — real
near-duplicate-sibling shape. Separately confirm `CardQuickView.kt` does NOT compose `CardBase(`
anywhere in its body (grep confirms zero matches) — it builds its own `Column`-based layout with
its own header/pin/favorite logic (`isPinned`/`isFavorite` params, no `CardBase` wrapping) — so
evaluate it as a distinct finding rather than assuming it belongs in the same 4-way group; state
this fact explicitly in the writeup. Disposition the TextCard/ListCard/AlbumCard/VoiceCard
4-way group and CardQuickView separately (unify / keep-with-rationale / prune each), citing each
component's actual domain content (text/list/album/voice previews) per the D-03 litmus's
domain-noun question. For any "unify" disposition, run
`grep -rl "<ComponentName>" ~/Projects/SecondBrain/app/src ~/Projects/CalTracker_Android/app/src`
(read-only, per D-02) and record each repo's file count inline.

Replace the `### Chips` placeholder: transcribe all 5 entries' `name` + shipped `tier` verbatim
from `ChipsFamilyScreen.kt`'s `chipsFamilyEntries` list. Evaluate two candidates: (1) `ChipBar`
vs `FilterBar` (both PRIMITIVE, both generic `<T>` containers rendering caller-supplied chip
content — `ChipBar` is a plain items-list container, `FilterBar` adds an expand/collapse
affordance around the same content shape) — does `FilterBar`'s expand/collapse belong as a
`ChipBar` mode instead of a sibling primitive; (2) `AppChip` vs `TagChipWithContextMenu` — `Read`
both files. Confirm (already verified this session) that `TagChipWithContextMenu` directly
composes `AppChip(...)` internally, forwarding 7 of `AppChip`'s params verbatim (`label`,
`isSelected`, `onClick`, `leadingIcon`, `trailingIcon`, `relatednessStrength`, `onDoubleClick`)
and adding 4 new menu-specific params (`onEdit`, `onRemoveFromContext`, `onDelete`,
`removeLabel`), while wrapping `onLongClick` locally to open its `DropdownMenu` — a
decorator/wrapper relationship, not two independent implementations of the same rendering logic.
Cite this fact and disposition accordingly (do not assume the disposition — read and judge). For
any "unify" disposition among these, run the same D-02 blast-radius grep and record counts.

If either family surfaces a genuinely new cross-entry altitude-mismatch candidate (not already
resolved by Phase 1 — see `docs/DESIGN-INTENT.md`'s worked examples and `ChipsFamilyScreen.kt`'s
inline note on `AppChip`'s prior PRIMITIVE-to-PATTERN correction), apply the D-03 litmus
explicitly (state both yes/no answers) and disposition it; otherwise do not restate settled tier
values as if they were new findings.
  </action>
  <verify>
    <automated>grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md | grep -qx 8 && grep -q "CardQuickView" docs/COHERENCE-AUDIT.md && grep -q "TagChipWithContextMenu" docs/COHERENCE-AUDIT.md</automated>
  </verify>
  <done>Cards (11 entries) and Chips (5 entries) sections are fully enumerated with name+tier, every raised finding (the TextCard/ListCard/AlbumCard/VoiceCard 4-way group, CardQuickView, ChipBar/FilterBar, AppChip/TagChipWithContextMenu) carries a unify/keep-with-rationale/prune disposition with cited rationale, every "unify" disposition among them carries per-repo blast-radius counts, and the pending-placeholder count has dropped from 10 to 8.</done>
</task>

<task type="auto">
  <name>Task 3: Audit Sheets + Tactile Foundation families — enumerate, disposition, blast-radius</name>
  <files>docs/COHERENCE-AUDIT.md</files>
  <action>
Replace the `### Sheets` placeholder: transcribe all 18 entries' `name` + shipped `tier` verbatim
from `SheetsFamilyScreen.kt`'s `sheetsFamilyEntries` list. `Read` the 3 same-base-name pairs'
actual signatures: `TagPickerSheet`/`TagPickerSheetContent`, `BulkCreatePopup`/
`BulkCreatePopupContent`, `TagCreateSheet`/`TagCreateSheetContent`. Confirm each pair is the
established shell/body split (a modal-chrome wrapper composable + its content-only variant, both
independently registered) — RESEARCH.md's Pitfall 3 flags this as the intentional precedent, not
a duplicate — and disposition each pair keep-with-rationale unless your own read of the actual
files contradicts that reading. Scan the remaining 12 non-paired Sheets entries
(`AlbumSourcePickerSheet`, `AlbumTitleConfirmSheet`, `CardEditorShellContent`,
`ListCardBottomSheet`, `RecordingBottomSheetContent`, `SheetScaffold`, `TagChipEditorContent`,
`TextCardBottomSheet`, `NameAndTagsEditor`, `VoiceRenameTagsSheet`, `ClearableTextField`,
`EditorItemRow`) for any further overlap/near-duplicate/altitude candidate genuinely present in
their public signatures — state explicitly if none is found rather than leaving them unaddressed.

Replace the `### Tactile Foundation` placeholder: transcribe all 4 entries' `name` + shipped
`tier` from `TactileFoundationFamilyScreen.kt`'s `tactileFoundationFamilyEntries` list. `Read`
`GradientSwatch.kt` and `HeatSwatch.kt` (both PATTERN, both same-family `*Swatch` naming).
Confirm (per `docs/DESIGN-INTENT.md`'s own worked example) `HeatSwatch` hardcodes its own sample
data and renders a specific mindmap-relatedness visual convention, while `GradientSwatch` takes a
caller-supplied `accentColor: Color` and renders a generic accent-gradient ramp — different
purposes despite the naming/family/tier match. Disposition keep-with-rationale unless your own
read contradicts that reading (cite each signature, per Pitfall 3 — never disposition from the
registry listing's naming alone). Check `ElevationLadder` and `TactileTypeShowcase` for any
further finding; state explicitly if none is found.

For any "unify" disposition surfaced in either family, run the D-02 blast-radius grep
(`grep -rl "<ComponentName>" ~/Projects/SecondBrain/app/src ~/Projects/CalTracker_Android/app/src`,
read-only) and record per-repo counts inline.
  </action>
  <verify>
    <automated>grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md | grep -qx 6 && grep -q "GradientSwatch" docs/COHERENCE-AUDIT.md && grep -q "HeatSwatch" docs/COHERENCE-AUDIT.md && grep -q "TagPickerSheetContent" docs/COHERENCE-AUDIT.md</automated>
  </verify>
  <done>Sheets (18 entries) and Tactile Foundation (4 entries) sections are fully enumerated with name+tier, the 3 Sheet+Content shell/body pairs and the GradientSwatch/HeatSwatch pair are each addressed with a cited disposition, every remaining entry in both families is either dispositioned or explicitly stated to have no finding, and the pending-placeholder count has dropped from 8 to 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan writes only `docs/COHERENCE-AUDIT.md` from existing, already-published repo source (`*FamilyScreen.kt` files, `docs/DESIGN-INTENT.md`) and read-only greps against two on-disk consumer trees. No runtime code path, network call, auth/session/crypto surface, or user input is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `docs/COHERENCE-AUDIT.md` (blast-radius grep output) | low | accept | The doc records file counts/paths from `~/Projects/SecondBrain` and `~/Projects/CalTracker_Android` — both are already-named, non-secret consumer repos referenced openly elsewhere in this hub's own `CLAUDE.md`/`ECOSYSTEM.md`; no credentials, tokens, or private business data are read or written by the read-only grep. |

No other STRIDE categories apply — this plan performs no writes outside `docs/COHERENCE-AUDIT.md`, installs no dependencies, and processes no external/untrusted input.
</threat_model>

<verification>
- `for f in Cards Chips Sheets "Buttons / FAB" Pickers Feedback "Empty State" "Progress / Metrics" "Tactile Foundation" "Unify Work-Order"; do grep -q "^### $f\$" docs/COHERENCE-AUDIT.md || echo "MISSING: $f"; done` prints nothing after Task 1.
- `grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md` reads 10 after Task 1, 8 after Task 2, 6 after Task 3.
- Every `Entry.tier` value transcribed into the doc for Cards/Chips/Sheets/Tactile Foundation matches the shipped value in the corresponding `*FamilyScreen.kt` file (spot-checked by re-reading the source alongside the just-written section, per 02-VALIDATION.md's sampling rate).
</verification>

<success_criteria>
- `docs/COHERENCE-AUDIT.md` exists with the full 10-heading skeleton (9 families + Unify Work-Order).
- Cards, Chips, Sheets, and Tactile Foundation (38 of 53 entries) are fully enumerated with cited, dispositioned findings; every "unify" finding among them carries per-repo blast-radius counts.
- The remaining 6 sections (Buttons / FAB, Pickers, Feedback, Empty State, Progress / Metrics, Unify Work-Order) still carry the tracer's placeholder, ready for 02-02-PLAN.md.
</success_criteria>

<output>
Create `.planning/phases/02-coherence-audit/02-01-SUMMARY.md` when done
</output>