---
phase: 05-gardening-unify-coordinated-repin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/io/github/ygaray/yahirandroidtaste/component/ChipBar.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/component/FilterBar.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt
  - api.txt
  - API.md
autonomous: true
requirements: [GARD-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GARD-01 (WO-1): FilterBar is retired as a standalone registered component; its expand/collapse chrome lives on inside ChipBar's new expandable mode (docs/COHERENCE-AUDIT.md Finding CH-1 / Unify Work-Order WO-1)"
    - "ChipBar with items = emptyList() and expandable set renders the chevron alone inside the chrome Surface, with no dead space beyond it (UI-SPEC UI Considerations: empty)"
    - "ChipBar expanded with many chips is height-capped to ~40% of screen height and scrolls internally, matching FilterBar's pre-unify behavior exactly (UI-SPEC UI Considerations: overflow)"
    - "ChipBar collapsed with content overflowing one line clips at maxLines = 1 with no new ellipsis/truncation behavior (UI-SPEC UI Considerations: partial)"
    - "ComponentRegistryDriftGuardTest passes after FilterBar's registry Entry and FilterBarVariants() demo are removed — the registered-XOR-allowlisted invariant holds"
  artifacts:
    - "src/main/java/io/github/ygaray/yahirandroidtaste/component/ChipBar.kt (ExpandableConfig data class + expandable/rawContent trailing params)"
    - "src/main/java/io/github/ygaray/yahirandroidtaste/component/FilterBar.kt (deleted)"
    - "src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt (FilterBar Entry + FilterBarVariants() removed; ChipBarVariants() gains an expandable-mode demo)"
    - "api.txt (rebaselined: FilterBarKt class removed, ChipBarKt.ChipBar signature grows, new ExpandableConfig public type appears)"
    - "API.md (Chips table: FilterBar row removed, ChipBar row documents the new expandable/rawContent params)"
  key_links:
    - "ChipsFamilyScreen.kt's ChipBarVariants() demo -> ChipBar's new expandable param (the gallery keeps showcasing the fold-in behavior)"
    - "ChipBar.kt's ExpandableConfig? param -> ComponentRegistry's init{} registered-XOR-allowlisted invariant (ExpandableConfig is a data class, not a @Composable, so it is outside that invariant's scope entirely — confirm this is understood, not accidentally allowlisted)"
    - "api.txt rebaseline -> ./gradlew apiCheck gate this and every later plan in this phase depends on staying green"
  prohibitions:
    - "Implement exactly the two dispositioned unify tuples from docs/COHERENCE-AUDIT.md's Unify Work-Order — do not re-analyze, expand, or prune additional overlaps at execution time"
    - "Do not edit any consumer repo file (SecondBrain/CalTracker), and do not autonomously cut a git tag or bump a consumer's Gradle coordinate from this hub-scoped phase — the coordinated repin is a human-gated surfacing task only"
    - "Do not silently change any existing pixel/dp value, color-role, typography-role, or copy string of ChipBar — this phase relocates/folds code, it does not redesign it"
    - "Do not mark nyquist_compliant: true anywhere in this phase's own plan/summary artifacts — that field is finalizer-owned, post-execution only"
---

<objective>
Implement WO-1 of `docs/COHERENCE-AUDIT.md`'s Unify Work-Order: fold `FilterBar`'s expand/collapse
chrome into `ChipBar` as an optional `expandable` mode, retire `FilterBar` as a standalone
registered component, and rebaseline `api.txt`/`API.md` for the intentional break — end to end,
proving the fold -> registry -> apiDump -> apiCheck -> `HUB_LANE_OVERRIDE=3` commit mechanic this
whole phase reuses for WO-2 in `05-02-PLAN.md`.

Purpose: eliminates a sibling PRIMITIVE (`FilterBar`) that duplicates `ChipBar`'s FlowRow
chip-container shape, per Finding CH-1's disposition, and proves the intentional-API-break
mechanic (registry rebaseline, Metalava rebaseline, lane-3 commit gate) this phase relies on twice.
Output: `ChipBar` carries `FilterBar`'s chrome as an opt-in mode; `FilterBar.kt` is deleted;
`api.txt`/`API.md` reflect the unified public surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-CONTEXT.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-PATTERNS.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-UI-SPEC.md
@docs/COHERENCE-AUDIT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This section tracks every symbol the whole Phase 5 gardening effort creates or removes across all
three plans (05-01, 05-02, 05-03), so no downstream consumer of this plan set has to reconstruct
the full picture from three separate diffs.

- **`ChipBar`'s new `expandable: ExpandableConfig? = null` and `rawContent: (@Composable
  FlowRowScope.() -> Unit)? = null` trailing params** (this plan, Task 1) — both nullable/defaulted,
  every existing bare-mode call site unaffected.
- **New public `ExpandableConfig` data class** (this plan, Task 1) — `expanded`, `onExpand`,
  `onCollapse`, `contentDescription` (defaulted `"Tag filters"`). A data class, not a `@Composable`,
  so it is outside `ComponentRegistry`'s registered-XOR-allowlisted invariant entirely.
- **`FilterBar.kt` removed entirely** (this plan, Task 1) — no source-compat shim; its registry
  `Entry` and demo function are also removed from `ChipsFamilyScreen.kt`; no `FilterBar`
  `INTENTIONALLY_UNREGISTERED` entry is added (the symbol no longer exists at all).
- **New shared composable `SheetHeaderMenu`** (05-02-PLAN.md, not this plan) — declared `internal`,
  added to `ComponentRegistry.INTENTIONALLY_UNREGISTERED` with a one-line rationale.
- **`api.txt`/`API.md` rebaselined twice** — once by this plan (WO-1: `FilterBar`'s class removed,
  `ChipBar`'s signature grows, `ExpandableConfig` appears), once by 05-02-PLAN.md (WO-2: no new
  public symbol, both host sheets' signatures unchanged).

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Fold FilterBar's expand/collapse chrome into ChipBar; retire FilterBar's registry entry</name>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/component/ChipBar.kt, src/main/java/io/github/ygaray/yahirandroidtaste/component/FilterBar.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt</files>
  <read_first>
    src/main/java/io/github/ygaray/yahirandroidtaste/component/ChipBar.kt (current 65-line file — the
    bare FlowRow shape this task extends, never regresses for the expandable == null path);
    src/main/java/io/github/ygaray/yahirandroidtaste/component/FilterBar.kt (current 113-line file —
    the chrome being folded in, then deleted);
    src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt (the FilterBar
    Entry block, the FilterBarVariants() demo function, and ChipBarVariants());
    .planning/phases/05-gardening-unify-coordinated-repin/05-UI-SPEC.md section "WO-1 — ChipBar's new
    expandable mode" (the LOCKED signature, state matrix, and composition order — authoritative over
    RESEARCH.md's own "illustrative" sketch of the same shape);
    .planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md Pitfall 2 (the registry
    registered-XOR-allowlisted invariant fires at object-init time, failing every test that touches
    ComponentRegistry if this edit is left half-finished).
  </read_first>
  <action>
    In `ChipBar.kt`, add a public top-level `data class ExpandableConfig(val expanded: Boolean, val
    onExpand: () -> Unit, val onCollapse: () -> Unit, val contentDescription: String = "Tag
    filters")` and two new trailing, nullable, defaulted parameters on `ChipBar<T>`, appended after
    `trailingContent`: `expandable: ExpandableConfig? = null` and `rawContent: (@Composable
    FlowRowScope.() -> Unit)? = null`. When `expandable == null`, `ChipBar` must render byte-identical
    to its current bare shape (no `Surface`, both-axis `Arrangement.spacedBy(8.dp)`, no chevron) — every
    existing call site that never touches these two new params must keep compiling and rendering
    unchanged. When `expandable != null`, wrap the `FlowRow` in a `Surface` (`fillMaxWidth()`,
    `Modifier.semantics { contentDescription = expandable.contentDescription }`, `tonalElevation =
    1.dp`, `color = MaterialTheme.colorScheme.surface`) and give the inner `FlowRow`
    `padding(horizontal = 16.dp, vertical = 8.dp)`, `horizontalArrangement = spacedBy(8.dp)`,
    `verticalArrangement = spacedBy(2.dp)` (the tight-gap exception, not the bare mode's 8dp),
    `itemVerticalAlignment = Alignment.CenterVertically`, and `maxLines = 1` when collapsed or
    `Int.MAX_VALUE` when `expandable.expanded`; additionally, when expanded, chain
    `.heightIn(max = (LocalConfiguration.current.screenHeightDp * 0.4f).dp)` and
    `.verticalScroll(rememberScrollState())` onto that `FlowRow`'s modifier. Inside that `FlowRow`'s
    content lambda, compose in this exact order whenever `expandable != null`: (1) a chevron
    `IconButton` (`onClick` = `expandable.onCollapse` when expanded else `expandable.onExpand`,
    `Modifier.semantics { contentDescription = "Show fewer tags" }` when expanded else `"Show all
    tags"`) wrapping an `Icon` (`Icons.Default.KeyboardArrowUp`/`KeyboardArrowDown`,
    `contentDescription = null`); (2) `leadingContent?.invoke()`; (3) exactly one of the two mutually
    exclusive body-content modes — `items.forEach { item -> key(key(item)) { itemContent(item) } }`
    for the typed path, or `rawContent?.invoke(this)` for the freeform path a caller migrating off
    the folded-in composable passes `items = emptyList()` plus `rawContent = { ... }` to reach; (4)
    `trailingContent?.invoke()`. Merge the needed imports from the composable being folded in
    (`Icons.Default.KeyboardArrowDown`/`KeyboardArrowUp`, `IconButton`, `Surface`, `MaterialTheme`,
    `LocalConfiguration`, `heightIn`, `verticalScroll`, `rememberScrollState`, `semantics`/
    `contentDescription`, `Alignment`) into `ChipBar.kt`. Extend `ChipBar`'s KDoc `@param` list with
    `expandable` and `rawContent`, and note in the KDoc body that this composable now also owns the
    former sibling's expand/collapse chrome (cite WO-1 / `docs/COHERENCE-AUDIT.md` Finding CH-1).
    <!-- planner-discipline-allow: FilterBar -->
    Delete `component/FilterBar.kt` entirely — UI-SPEC's locked default is full retirement with no
    source-compat forwarding shim (this is a hub-only break already gated behind the human-gated
    `v2.0.0` coordinated repin in `05-03-PLAN.md`, so no shim is warranted). Then, in
    `explorer/ChipsFamilyScreen.kt`: remove the `import
    io.github.ygaray.yahirandroidtaste.component.FilterBar` line; remove the fifth `Entry(...)` block
    in `chipsFamilyEntries` (the one whose `name` names the retired sibling); remove the private
    demo function that showcased it; remove the now-unused `import
    io.github.ygaray.yahirandroidtaste.model.TagChipUiModel` line (re-grep this file after the above
    removals to confirm no reference survives before deleting the import — it was only used by that
    demo's four call sites). Extend `ChipBarVariants()` with a new demo pair exercising the
    `expandable` mode — a collapsed-state section and an expanded-state section (mirroring what the
    removed demo showed: a short chip set collapsed, `ExplorerFakeData.manyTagChips` expanded), each
    preceded by a `SectionLabel(...)` matching this file's existing style.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*ComponentRegistryDriftGuardTest*" --tests "*ComponentRegistryTierTest*" --tests "*ComponentRegistrySearchTest*"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/main/java/io/github/ygaray/yahirandroidtaste/component/FilterBar.kt` (file deleted)
    - `grep -c 'FilterBar' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt` equals 0
    - `grep -c 'expandable: ExpandableConfig? = null' src/main/java/io/github/ygaray/yahirandroidtaste/component/ChipBar.kt` equals 1
    - `grep -c 'rawContent' src/main/java/io/github/ygaray/yahirandroidtaste/component/ChipBar.kt` is >= 1
    - `./gradlew testDebugUnitTest --tests "*ComponentRegistryDriftGuardTest*"` passes (registered-XOR-allowlisted invariant holds with the sibling fully gone, not orphaned in either list)
  </acceptance_criteria>
  <reversibility rating="one-way">
    Deleting FilterBar.kt outright (no source-compat shim) is a public-API-breaking, one-way choice —
    already resolved as UI-SPEC's locked default ("Overridable by planner if a staged-migration shim
    is judged necessary... flagged here as the locked default, not a hard mandate"). Not gated behind
    a checkpoint:decision here: the break itself is safely contained behind the separate, blocking
    human-gated tag-cut checkpoint in 05-03-PLAN.md — nothing in this task ships to a consumer.
  </reversibility>
  <done>ChipBar owns the folded-in expand/collapse chrome as an opt-in `expandable` mode; every existing bare-mode call site is unaffected; FilterBar.kt is deleted; the gallery still showcases the expand/collapse behavior via ChipBarVariants(); ComponentRegistryDriftGuardTest passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rebaseline api.txt/API.md for WO-1 and commit via the lane-3 gate</name>
  <files>api.txt, API.md</files>
  <read_first>
    api.txt lines around the current ChipBarKt/FilterBarKt entries (grep for `ChipBarKt` and the
    class the deleted composable's file compiled to, to find current line numbers — do not assume
    RESEARCH.md's session-stale line numbers still hold);
    API.md's Chips section (`## 3. Chips`) — the current hand-written table rows for `ChipBar` and
    the retired composable;
    tools/hooks/pre-commit (the `HUB_LANE_OVERRIDE=3` mechanic this and every later source commit in
    this phase must use);
    .planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md Pitfall 3 (apiDump must be
    reviewed line-by-line, not blindly regenerated) and Pitfall 4 (the pre-commit lane gate).
  </read_first>
  <action>
    Run `./gradlew apiDump` to regenerate `api.txt`, then review `git diff api.txt` line by line:
    confirm the class the deleted composable's file used to compile to is gone; confirm
    `ChipBarKt.ChipBar`'s method line gained exactly the two new optional trailing params
    (`expandable`, `rawContent`) and nothing else in that line changed; confirm a new public
    `ExpandableConfig`-related class/constructor entry appears (it is a public data class whose
    presence in `api.txt` is intentional, since `ChipBar`'s own public signature now exposes it);
    confirm no other symbol anywhere else in the file changed — if `apiDump` surfaces any unrelated
    change, stop and investigate before proceeding, per Pitfall 3. Update `API.md`'s Chips table:
    <!-- planner-discipline-allow: FilterBar -->
    delete the retired composable's row entirely, and rewrite the `ChipBar` row's description column
    to also cover the new `expandable`/`rawContent` params, mirroring the two-state-contract phrasing
    the Sheets table rows already use for `onEditRequest`. Run `./gradlew testDebugUnitTest detekt
    apiCheck` and confirm all three succeed with zero failures and zero detekt findings (zero-baseline
    policy). Stage `ChipBar.kt`, the deleted file, `ChipsFamilyScreen.kt`, `api.txt`, and `API.md`,
    then commit with `HUB_LANE_OVERRIDE=3 git commit -m "feat(chips): fold FilterBar into ChipBar's
    expandable mode (WO-1, GARD-01)"` — this is an intentional lane-3 (non-additive) change per
    `tools/hooks/pre-commit`'s classifier, so the override is required and expected, not a bypass.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest detekt apiCheck</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew apiCheck` exits 0 against the freshly committed `api.txt`
    - `grep -c 'ExpandableConfig' api.txt` is >= 1
    - `grep -c 'FilterBar' API.md` equals 0
    - `git show --stat HEAD` lists `api.txt`, `API.md`, `ChipBar.kt`, `ChipsFamilyScreen.kt`, and the deleted file
    - `git log -1 --pretty=%B` references `GARD-01`
  </acceptance_criteria>
  <reversibility rating="costly">
    Rebaselining api.txt is the settled mechanic per CONTEXT.md D-02 (apiDump + line-by-line review) —
    not gated; flagged only because a sloppy rebaseline is expensive to unwind after the tag cuts.
  </reversibility>
  <done>api.txt is rebaselined and apiCheck passes clean; API.md's Chips table reflects the unified shape; the commit landed via HUB_LANE_OVERRIDE=3 with GARD-01 referenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is a same-package Compose UI refactor (folding already-shipped, already-reviewed rendering logic within `component/`) plus a `ComponentRegistry`/`api.txt` bookkeeping update. It introduces no new network call, no new persisted data, no new external input-parsing surface, and touches no secret/credential material (the hub holds none, per CLAUDE.md's own invariant). RESEARCH.md's Security Domain section reached the same conclusion after reviewing every applicable ASVS L1 category (V2/V3/V4/V6: not applicable to this UI-library refactor). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `api.txt` rebaseline (`./gradlew apiDump`) | medium | mitigate | Line-by-line diff review is a required task step (Task 2), not a rubber-stamped regenerate — confirms only the intended symbols changed before commit, catching an accidental unrelated symbol leak. |
| T-05-03 | Tampering | Pre-commit lane-gate override (`HUB_LANE_OVERRIDE=3`) | low | accept | Intentional, human-legible coordination gate, not a vulnerability — this commit IS a real lane-3 (non-additive) change, so the override is the correct, expected mechanism. |
| T-05-04 | (informational — pre-existing, not introduced here) | `verify-api-additive.sh` absolute-vs-relative path bug (pre-commit's own lane-3 API-break check silently no-ops) | medium | accept | Tracked in `.planning/STATE.md` Blockers/Concerns since before Phase 3 (commit `534ec10`); out of this phase's scope to fix. The `./gradlew apiCheck` Gradle task this plan relies on is a separate, working mechanism, unaffected by that bug. |
| T-05-05 | Tampering / Spoofing | Git tag `v2.0.0` cut + consumer coordinate bump (deferred to `05-03-PLAN.md`) | high | mitigate | Never executed autonomously anywhere in this phase — gated behind a blocking `checkpoint:decision` in `05-03-PLAN.md` requiring the human's explicit go-ahead, per CLAUDE.md's human-gated shipping rule; tags are immutable and consumers pin an immutable tag, never a moving ref. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — this plan adds no new third-party dependency (RESEARCH.md Package Legitimacy Audit: "Not applicable"). |

</threat_model>

<verification>
Run `./gradlew testDebugUnitTest --tests "*ComponentRegistryDriftGuardTest*"` after Task 1's source
edits land (before touching api.txt), then `./gradlew testDebugUnitTest detekt apiCheck` after
Task 2's rebaseline. Both must be green. This maps to ROADMAP Phase 5 success criteria 1-2 for the
WO-1 half of GARD-01.
</verification>

<success_criteria>
1. `ChipBar` carries FilterBar's expand/collapse chrome as an opt-in `expandable` mode; every
   existing bare-mode call site is unaffected.
2. FilterBar is retired: `FilterBar.kt` is deleted, its registry `Entry` and demo are removed.
3. `ComponentRegistryDriftGuardTest`, `testDebugUnitTest`, `detekt`, and `apiCheck` all pass clean
   against the rebaselined `api.txt`.
4. `API.md` documents the unified shape with no stale FilterBar mention.
</success_criteria>

<output>
Create `.planning/phases/05-gardening-unify-coordinated-repin/05-01-SUMMARY.md` when done
</output>
