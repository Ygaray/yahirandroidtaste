---
phase: 05-gardening-unify-coordinated-repin
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/main/java/io/github/ygaray/yahirandroidtaste/component/SheetHeaderMenu.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/component/TextCardBottomSheet.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/component/ListCardBottomSheet.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistry.kt
  - src/test/java/io/github/ygaray/yahirandroidtaste/component/TextListBottomSheetEditMenuSourceContractTest.kt
  - api.txt
  - API.md
autonomous: true
requirements: [GARD-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GARD-01 (WO-2): TextCardBottomSheet and ListCardBottomSheet's shared header-Row + three-dot-menu + rename-dialog is extracted into one new composable both sheets compose; both sheets remain registered and unchanged in their own public shape (docs/COHERENCE-AUDIT.md Finding S-1 / Unify Work-Order WO-2)"
    - "Sheet header title renders with no maxLines/truncation — wraps naturally exactly as today (UI-SPEC UI Considerations: long-text)"
    - "imageCount 0/1/N preserves ImageCountIndicator's existing pluralization and zero-no-op behavior untouched (UI-SPEC UI Considerations: zero-one-many)"
    - "Both sheets' isPinned/isFavorite 4-state combinations (neither/pinned-only/favorite-only/both) render with unchanged icon show/hide logic, now shared (UI-SPEC UI Considerations: populated)"
    - "Rename dialog empty/whitespace-only input silently no-ops the confirm via the existing trim guard — no new error copy (UI-SPEC UI Considerations: error)"
    - "Delete (both sheets) fires immediately with no confirmation dialog — none is added by this extraction (UI-SPEC UI Considerations: destructive)"
    - "Rename dialog long title text relies on ClearableTextField's own existing wrap/scroll behavior, untouched by this phase (UI-SPEC UI Considerations: overflow)"
    - "TextListBottomSheetEditMenuSourceContractTest passes retargeted at the new shared file for the moved assertions, proving the region-marker/branch-logic coverage was not silently dropped"
  artifacts:
    - "src/main/java/io/github/ygaray/yahirandroidtaste/component/SheetHeaderMenu.kt (new — header Row + three-dot DropdownMenu + rename AlertDialog, internal, INTENTIONALLY_UNREGISTERED)"
    - "src/main/java/io/github/ygaray/yahirandroidtaste/component/TextCardBottomSheet.kt (shrinks — delegates header/menu/rename to SheetHeaderMenu)"
    - "src/main/java/io/github/ygaray/yahirandroidtaste/component/ListCardBottomSheet.kt (shrinks — delegates header/menu/rename to SheetHeaderMenu)"
    - "src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistry.kt (INTENTIONALLY_UNREGISTERED gains a SheetHeaderMenu entry with a one-line rationale)"
    - "src/test/java/io/github/ygaray/yahirandroidtaste/component/TextListBottomSheetEditMenuSourceContractTest.kt (retargeted at the new file boundary)"
    - "api.txt (rebaselined: TextCardBottomSheetKt/ListCardBottomSheetKt signatures unchanged, no new public SheetHeaderMenu symbol)"
    - "API.md (Intentionally-unregistered sub-parts table gains a SheetHeaderMenu row, count bumped)"
  key_links:
    - "TextCardBottomSheet.kt / ListCardBottomSheet.kt -> SheetHeaderMenu(...) call (both sheets wire the shared composable, forwarding their own onEditRequest/imageCount)"
    - "ComponentRegistry's registered-XOR-allowlisted init{} invariant -> SheetHeaderMenu's INTENTIONALLY_UNREGISTERED entry"
    - "TextListBottomSheetEditMenuSourceContractTest's region-marker anchors -> the // region:edit-menu-item / // endregion:edit-menu-item comments now living in SheetHeaderMenu.kt"
  prohibitions:
    - "Implement exactly the two dispositioned unify tuples from docs/COHERENCE-AUDIT.md's Unify Work-Order — do not re-analyze, expand, or prune additional overlaps at execution time"
    - "Do not edit any consumer repo file (SecondBrain/CalTracker), and do not autonomously cut a git tag or bump a consumer's Gradle coordinate from this hub-scoped phase — the coordinated repin is a human-gated surfacing task only"
    - "Do not silently change any existing pixel/dp value, color-role, typography-role, or copy string of TextCardBottomSheet/ListCardBottomSheet, and do not add a Delete confirmation dialog that doesn't exist today — UI-SPEC's preserve-exactly contract is locked"
    - "Do not let the WO-2 extraction silently drop TextListBottomSheetEditMenuSourceContractTest's region-marker/branch-logic coverage, and do not mark nyquist_compliant: true anywhere in this phase's own plan/summary artifacts (finalizer-owned, post-execution only)"
---

<objective>
Implement WO-2 of `docs/COHERENCE-AUDIT.md`'s Unify Work-Order: extract the byte-identical shared
header-Row + three-dot-menu + rename-dialog block duplicated across `TextCardBottomSheet` and
`ListCardBottomSheet` into one new shared composable, wire both sheets to it, retarget the one
existing test whose assertions this extraction moves, and rebaseline `api.txt`/`API.md`.

Purpose: eliminates the last non-trivial duplication Finding S-1 identified between the two
near-duplicate sheets (both already share their body region via `CardQuickView`, per D-04's
precedent — this closes the remaining header/menu/dialog gap the same way).
Output: a new `SheetHeaderMenu` composable both sheets compose; `api.txt`/`API.md` reflect the
unchanged public shape of both host sheets plus the new internal, allowlisted sub-part.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-CONTEXT.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-PATTERNS.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-UI-SPEC.md
@docs/COHERENCE-AUDIT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This section tracks every symbol the whole Phase 5 gardening effort creates or removes across all
three plans (05-01, 05-02, 05-03), so no downstream consumer of this plan set has to reconstruct
the full picture from three separate diffs.

- **New shared composable `SheetHeaderMenu`** (this plan, Task 1) — `@Composable internal fun
  SheetHeaderMenu(title, isPinned, isFavorite, onTogglePin, onToggleFavorite, onDelete, onDismiss,
  onConfirmRename, onEditRequest = null, imageCount = 0, modifier = Modifier)`, owning the header
  Row + three-dot `DropdownMenu` + rename `AlertDialog` triad moved out of both bottom sheets.
- **New `ComponentRegistry.INTENTIONALLY_UNREGISTERED["SheetHeaderMenu"]` entry** (this plan,
  Task 1) — one-line rationale mirroring the existing `SwipeableActionRow` allowlist entry.
- **`TextCardBottomSheet.kt`/`ListCardBottomSheet.kt` shrink** (this plan, Task 1) — both delegate
  to `SheetHeaderMenu(...)`; their own public signatures are unchanged.
- **`TextListBottomSheetEditMenuSourceContractTest.kt` retargeted** (this plan, Task 2) — the moved
  assertions now check `source("SheetHeaderMenu.kt")` instead of the two host sheets.
- **`ChipBar`'s new `expandable`/`rawContent` params + `ExpandableConfig` data class +
  `FilterBar.kt` removal** — produced by 05-01-PLAN.md, not this plan.
- **`api.txt`/`API.md` rebaselined** (this plan, Task 2) — no new public symbol for
  `SheetHeaderMenu` (stays `internal`); both host sheets' public signatures byte-identical to their
  pre-extraction baseline.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract SheetHeaderMenu and wire both sheets to it</name>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/component/SheetHeaderMenu.kt, src/main/java/io/github/ygaray/yahirandroidtaste/component/TextCardBottomSheet.kt, src/main/java/io/github/ygaray/yahirandroidtaste/component/ListCardBottomSheet.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistry.kt</files>
  <read_first>
    src/main/java/io/github/ygaray/yahirandroidtaste/component/TextCardBottomSheet.kt (full file —
    the header Row, three-dot DropdownMenu, and rename AlertDialog blocks being moved out, plus the
    categoryPath/CardQuickView/Button code that stays);
    src/main/java/io/github/ygaray/yahirandroidtaste/component/ListCardBottomSheet.kt (full file —
    the same three blocks, byte-identical to TextCardBottomSheet's except it never renders an
    image-count indicator);
    src/main/java/io/github/ygaray/yahirandroidtaste/component/CardQuickView.kt lines 1-55 (the
    KDoc citation-pattern precedent this new file's own KDoc should mirror — D-04's extraction
    rationale style);
    src/main/java/io/github/ygaray/yahirandroidtaste/component/ImageCountIndicator.kt (confirms it
    is `internal` and already no-ops — renders nothing, reserves no space — at `imageCount <= 0`,
    which is why the new composable can call it unconditionally without a new conditional branch);
    .planning/phases/05-gardening-unify-coordinated-repin/05-UI-SPEC.md section "WO-2 — Shared
    sheet header + three-dot menu + rename dialog" (the LOCKED signature, state matrix, internal-
    state ownership, extraction boundary, and region-marker requirement — authoritative);
    .planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md Pitfall 1 (the exact test
    breakage this extraction causes if the region markers are dropped) and the "ImageCountIndicator
    is TextCardBottomSheet-only" anti-pattern note.
  </read_first>
  <action>
    Create `src/main/java/io/github/ygaray/yahirandroidtaste/component/SheetHeaderMenu.kt`, package
    `io.github.ygaray.yahirandroidtaste.component`, declaring `@Composable internal fun
    SheetHeaderMenu(title: String, isPinned: Boolean, isFavorite: Boolean, onTogglePin: () -> Unit,
    onToggleFavorite: () -> Unit, onDelete: () -> Unit, onDismiss: () -> Unit, onConfirmRename:
    (String) -> Unit, onEditRequest: (() -> Unit)? = null, imageCount: Int = 0, modifier: Modifier =
    Modifier)` exactly per UI-SPEC's locked signature. Its body is the verbatim union of both
    sheets' current header `Row` (title `Text` gated on non-blank, Pin icon gated on `isPinned`,
    Favorite icon gated on `isFavorite`, then an unconditional `ImageCountIndicator(imageCount,
    modifier = Modifier.padding(top = 4.dp, end = 4.dp))` positioned after Favorite and before the
    three-dot menu — unconditional per UI-SPEC since the indicator already no-ops at `imageCount <=
    0`, not a new conditional branch), the three-dot `IconButton` + `DropdownMenu` (Edit ->
    Pin/Unpin -> Favorite/Unfavorite -> Delete, identical icons/colors/error-tint to today), and the
    rename `AlertDialog` block — moved in verbatim, since both source files already carry this code
    byte-identically. Move the region marker comment pair that wraps the Edit menu row into this
    new file verbatim, wrapping the same item — do not drop them. Move the three pieces of local
    state (`showMenu`, `showRenameDialog`, `renameText` re-keyed on `title`) into this composable —
    they are no longer hoisted in either host sheet. Write a KDoc header mirroring
    `CardQuickView.kt`'s citation style: state the extraction rationale citing WO-2 /
    `docs/COHERENCE-AUDIT.md` Finding S-1, document each param, and state explicitly this archetype
    owns only the header Row + three-dot menu + rename dialog triad — not the sheet body, and not
    the categoryPath line (that line stays in each host sheet, outside this extraction's boundary,
    per UI-SPEC). In `TextCardBottomSheet.kt`, replace the entire header `Row{...}` block and the
    trailing local rename `AlertDialog` block with a single call to the new composable, forwarding
    `title`, `isPinned`, `isFavorite`, `onTogglePin`, `onToggleFavorite`, `onDelete`, `onDismiss`,
    `onConfirmRename`, `onEditRequest`, and `imageCount`; remove the now-hoisted-away
    `showMenu`/`showRenameDialog`/`renameText` local declarations; leave `TextCardBottomSheet`'s own
    signature, its `categoryPath` `Text`, its `CardQuickView(...)` call, and its "Edit note" `Button`
    untouched. In `ListCardBottomSheet.kt`, make the identical replacement, omitting `imageCount`
    from the call (defaults to 0, reproducing this sheet's current no-indicator appearance via the
    indicator's own no-op, not a new branch); leave its `categoryPath`, `CardQuickView(...)`,
    "Edit list" `Button`, and `ListPreviewItemRow` untouched. Remove now-unused imports from both
    host files after the move (the Kotlin compiler and detekt's zero-baseline `UnusedImports` rule
    will surface anything missed) — this typically includes `AlertDialog`, `DropdownMenu`,
    `DropdownMenuItem`, `Box`, the menu/pin/favorite/delete icon imports, `Alignment`, `TextButton`,
    and `remember`/`mutableStateOf`/`getValue`/`setValue` if nothing else in that file still uses
    them. Finally, in `explorer/ComponentRegistry.kt`, add one new `INTENTIONALLY_UNREGISTERED`
    entry — `"SheetHeaderMenu" to` a one-line reason stating it is header/menu/rename chrome
    extracted from both bottom sheets, infrastructure rather than an independently showcase-able
    archetype, already exercised indirectly via every sheet entry's own header/menu/rename
    interaction — mirroring the existing `SwipeableActionRow` entry's exact shape. This is UI-SPEC's
    locked default (internal + allowlisted, not independently registered); do not register it as a
    standalone gallery `Entry` instead.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*ComponentRegistryDriftGuardTest*"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/main/java/io/github/ygaray/yahirandroidtaste/component/SheetHeaderMenu.kt`
    <!-- planner-discipline-allow: // region:edit-menu-item -->
    <!-- planner-discipline-allow: // endregion:edit-menu-item -->
    - `grep -c '// region:edit-menu-item' src/main/java/io/github/ygaray/yahirandroidtaste/component/SheetHeaderMenu.kt` equals 1
    - `grep -c '// region:edit-menu-item' src/main/java/io/github/ygaray/yahirandroidtaste/component/TextCardBottomSheet.kt` equals 0
    - `grep -c '// region:edit-menu-item' src/main/java/io/github/ygaray/yahirandroidtaste/component/ListCardBottomSheet.kt` equals 0
    - `grep -c 'SheetHeaderMenu(' src/main/java/io/github/ygaray/yahirandroidtaste/component/TextCardBottomSheet.kt` is >= 1
    - `grep -c 'SheetHeaderMenu(' src/main/java/io/github/ygaray/yahirandroidtaste/component/ListCardBottomSheet.kt` is >= 1
    - `grep -c '"SheetHeaderMenu" to' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistry.kt` equals 1
    - `./gradlew testDebugUnitTest --tests "*ComponentRegistryDriftGuardTest*"` passes
  </acceptance_criteria>
  <reversibility rating="one-way">
    Declaring SheetHeaderMenu `internal` + `INTENTIONALLY_UNREGISTERED` (vs. public + independently
    registered, mirroring CardQuickView) is a real design fork UI-SPEC already resolved as its locked
    default ("either choice is contract-compliant, but pick one and apply it consistently"). Not
    gated behind a checkpoint:decision — the choice is documented here and applied uniformly rather
    than re-litigated at execution time.
  </reversibility>
  <done>SheetHeaderMenu.kt exists and owns the header/menu/rename triad; both sheets delegate to it, forwarding their own onEditRequest/imageCount; ComponentRegistry's registered-XOR-allowlisted invariant holds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Retarget the source-contract test, rebaseline api.txt/API.md, and commit</name>
  <files>src/test/java/io/github/ygaray/yahirandroidtaste/component/TextListBottomSheetEditMenuSourceContractTest.kt, api.txt, API.md</files>
  <read_first>
    src/test/java/io/github/ygaray/yahirandroidtaste/component/TextListBottomSheetEditMenuSourceContractTest.kt
    (full file — every assertion's current `source(...)` target and exactly what each checks);
    src/test/java/io/github/ygaray/yahirandroidtaste/component/SourceContractTestSupport.kt (confirms
    `source(file)` resolves relative to `src/main/java/io/github/ygaray/yahirandroidtaste/component/`
    — so `source("SheetHeaderMenu.kt")` resolves correctly once Task 1's new file exists);
    api.txt (current state, post 05-01's rebaseline);
    API.md's "Intentionally-unregistered sub-parts" section (the current row count and the
    `SwipeableActionRow` row's exact format to mirror);
    .planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md Pitfall 1 (the precise
    failure mode this retarget must prevent) and Pitfall 3 (line-by-line apiDump review).
  </read_first>
  <action>
    Retarget `TextListBottomSheetEditMenuSourceContractTest.kt` to the new file boundary Task 1
    created. Keep the two "declares a trailing nullable defaulted onEditRequest param" assertions
    pointed at each host sheet's own file — that parameter is still each sheet's own signature,
    forwarded into the shared composable, unchanged. Retarget every assertion whose subject moved —
    the Edit-row branch check, the rename-dialog-state check, the `AlertDialog(` structural check,
    the `onConfirmRename(` invocation check, and the region-scoped exactly-one-Edit/zero-Rename
    label check — at the new shared file instead of either host sheet; since both sheets now share
    one implementation, collapse what were previously duplicated per-sheet assertion pairs into
    single assertions against the shared file rather than keeping two copies that check the same
    underlying source. Keep `ListCardBottomSheet`'s own "Edit list" button-copy assertion pointed at
    its own file (unmoved — outside the extraction boundary). Rework the final cross-file
    backward-compatibility test so it verifies both host files still declare their own
    `onEditRequest` param and call the shared composable (proving the wiring survived the
    extraction), while the rename-dialog/state structural presence is asserted once against the
    shared file rather than duplicated per host file. Update this test class's own KDoc closing
    paragraph to describe the new file boundary accurately (it currently narrates a plan/task
    structure that predates this extraction). Run `./gradlew apiDump`, then review `git diff api.txt`
    line by line: confirm no new public symbol appears for the extracted composable (it is declared
    `internal`, so Metalava must not add an entry for it — if one appears, the visibility modifier
    was dropped by mistake, fix before proceeding); confirm both host sheets' method signatures are
    byte-identical to their pre-Task-1 baseline (only their bodies changed, never their public
    shape); confirm no other symbol changed. Update `API.md`: bump the "Intentionally-unregistered
    sub-parts" heading's count by one and add a row for the new composable using the exact allowlist
    reason string from `ComponentRegistry.kt`'s new map entry, matching the existing
    `SwipeableActionRow` row's format. Run `./gradlew testDebugUnitTest detekt apiCheck` and confirm
    all three succeed. Stage the new file, both modified sheets, `ComponentRegistry.kt`, the
    retargeted test, `api.txt`, and `API.md`, then commit with `HUB_LANE_OVERRIDE=3 git commit -m
    "refactor(sheets): extract shared header/menu/rename composable (WO-2, GARD-01)"`.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest detekt apiCheck</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*TextListBottomSheetEditMenuSourceContractTest*"` passes
    - `./gradlew apiCheck` exits 0
    - `grep -c 'SheetHeaderMenuKt' api.txt` equals 0 (internal, must not leak into the public API)
    - `grep -c 'SheetHeaderMenu' API.md` is >= 1
    - `git show --stat HEAD` includes the retargeted test file, `api.txt`, `API.md`, `ComponentRegistry.kt`, `SheetHeaderMenu.kt`, `TextCardBottomSheet.kt`, and `ListCardBottomSheet.kt`
    - `git log -1 --pretty=%B` references `GARD-01`
  </acceptance_criteria>
  <reversibility rating="costly">
    Rebaselining api.txt a second time in this phase is the same settled mechanic as 05-01 (apiDump +
    line-by-line review, D-02) — not gated; flagged only because a sloppy rebaseline compounds with
    05-01's before the tag cuts.
  </reversibility>
  <done>The retargeted source-contract test passes against the new file boundary; api.txt/apiCheck confirm the extracted composable stayed internal and neither host sheet's public signature changed; API.md documents the new allowlisted sub-part; the commit landed via HUB_LANE_OVERRIDE=3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is a same-package Compose UI refactor (moving already-shipped, already-reviewed header/menu/rename-dialog rendering logic between files within `component/`) plus a `ComponentRegistry`/`api.txt` bookkeeping update. It introduces no new network call, no new persisted data, and touches no secret/credential material. The rename dialog's text input (`ClearableTextField`) is a pre-existing, unchanged-in-shape primitive that is only relocated by this plan, not reshaped — RESEARCH.md's Security Domain section marks this "marginal" under ASVS V5 for exactly that reason. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `api.txt` rebaseline (`./gradlew apiDump`) | medium | mitigate | Line-by-line diff review is a required task step (Task 2) — confirms the extracted composable stayed `internal` and neither host sheet's public shape changed before commit. |
| T-05-02 | Elevation of Privilege | `SheetHeaderMenu`'s public-vs-internal visibility choice | low | mitigate | Declared `internal` per UI-SPEC's locked default and verified by `apiCheck` (no public symbol for it in `api.txt`) — an accidental public leak of extracted rename/menu internals is caught by the same apiCheck gate, not left to review alone. |
| T-05-03 | Tampering | Pre-commit lane-gate override (`HUB_LANE_OVERRIDE=3`) | low | accept | Intentional, human-legible coordination gate — this commit IS a real lane-3 (non-additive) change, so the override is the correct, expected mechanism. |
| T-05-04 | (informational — pre-existing, not introduced here) | `verify-api-additive.sh` absolute-vs-relative path bug | medium | accept | Tracked in `.planning/STATE.md` Blockers/Concerns since before Phase 3; out of this phase's scope to fix. `./gradlew apiCheck` is a separate, working mechanism this plan relies on, unaffected by that bug. |
| T-05-05 | Tampering / Spoofing | Git tag `v2.0.0` cut + consumer coordinate bump (deferred to `05-03-PLAN.md`) | high | mitigate | Never executed autonomously anywhere in this phase — gated behind a blocking `checkpoint:decision` in `05-03-PLAN.md`; tags are immutable and consumers pin an immutable tag, never a moving ref. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — this plan adds no new third-party dependency. |

</threat_model>

<verification>
Run `./gradlew testDebugUnitTest --tests "*ComponentRegistryDriftGuardTest*"` after Task 1's source
edits land, then `./gradlew testDebugUnitTest detekt apiCheck` after Task 2's test retarget and
rebaseline. Both must be green. This maps to ROADMAP Phase 5 success criteria 1-2 for the WO-2 half
of GARD-01.
</verification>

<success_criteria>
1. `SheetHeaderMenu` owns the header/menu/rename triad; both `TextCardBottomSheet` and
   `ListCardBottomSheet` delegate to it and keep their own registered `Entry` unchanged.
2. `TextListBottomSheetEditMenuSourceContractTest` passes retargeted at the new file boundary — the
   region-marker and branch-logic coverage is not silently dropped.
3. `testDebugUnitTest`, `detekt`, and `apiCheck` all pass clean against the rebaselined `api.txt`.
4. `API.md` documents the new allowlisted sub-part with no stale references.
</success_criteria>

<output>
Create `.planning/phases/05-gardening-unify-coordinated-repin/05-02-SUMMARY.md` when done
</output>
