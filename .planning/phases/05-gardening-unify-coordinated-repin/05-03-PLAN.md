---
phase: 05-gardening-unify-coordinated-repin
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements: [GARD-01, GARD-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ComponentRegistry's registered-XOR-allowlisted invariant holds for the final post-unify state: the retired composable appears in neither entries nor INTENTIONALLY_UNREGISTERED; SheetHeaderMenu appears only in INTENTIONALLY_UNREGISTERED"
    - "api.txt matches the intentional post-unify public API and ./gradlew apiCheck passes clean against it, proving the break is fully captured, not a residual mismatch"
    - "GARD-02's coordinated repin (tag cut, SecondBrain + CalTracker repinned and Gate-1 re-verified) is explicitly surfaced to the human for go-ahead, never executed autonomously from this hub-scoped phase"
  artifacts: []
  key_links:
    - "05-01/05-02's committed source state -> the final ./gradlew testDebugUnitTest detekt apiCheck gate this plan re-runs"
    - "This plan's checkpoint:decision -> ECOSYSTEM.md §7 + ~/.claude/context/workflows/repin.md (the human-executed repin ritual) + repin_status.py reconcile (Phase 4 tooling, post-repin verification)"
  prohibitions:
    - "Implement exactly the two dispositioned unify tuples from docs/COHERENCE-AUDIT.md's Unify Work-Order — do not re-analyze, expand, or prune additional overlaps at execution time"
    - "Do not edit any consumer repo file (SecondBrain/CalTracker), and do not autonomously cut a git tag or bump a consumer's Gradle coordinate from this hub-scoped phase — the coordinated repin is a human-gated surfacing task (Task 2) only, never auto-approved"
    - "Do not treat this plan's own verification pass as a substitute for the human's explicit go/hold decision in Task 2's checkpoint"
    - "Do not mark nyquist_compliant: true anywhere in this phase's own plan/summary artifacts — that field is finalizer-owned, post-execution only"
---

<objective>
Confirm the hub-side gardening work (`05-01`, `05-02`) is fully green on `main`, then surface —
never execute — the human-gated coordinated repin: a new immutable `v2.0.0` tag (semver-correct
major per D-03, the first true break in an all-additive `v1.0.0`-`v1.10.0` history), SecondBrain's
single-hop repin, and CalTracker's two-hop catch-up-then-gardening repin per D-05, each re-verified
at Gate-1 in its own consumer project.

Purpose: this is the phase's completion boundary. Per this repo's CLAUDE.md ("Changes here ripple
to every consumer — and shipping is human-gated") and D-04's repin-seam decision (the hub cuts the
tag on `main`; each consumer repin runs through that consumer's own channel, human-gated —
sequential-in-hub, no consumer worktrees), cutting a tag and repinning either consumer must never
happen autonomously from this hub project.
Output: a confirmed-green hub `main`, and an explicit human go/hold decision recorded for the tag
cut + coordinated repin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-CONTEXT.md
@.planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md
@docs/COHERENCE-AUDIT.md
@CLAUDE.md
@ECOSYSTEM.md
</context>

<human_gated_boundary>
ROADMAP Phase 5 success criteria 3 ("A new immutable tag is cut") and 4 ("Both SecondBrain and
CalTracker are repinned...") are satisfied by this plan as **hub-side readiness proven +
human-gated surfacing**, not as autonomously-completed criteria. Task 1 below proves criteria 1-2
(unify implemented, drift-guard + apiCheck pass). Task 2 surfaces criteria 3-4 as a blocking
decision for the human to act on outside this plan's own execution — see
`~/.claude/context/workflows/repin.md` and `ECOSYSTEM.md` §7 for the ritual itself, and
`repin_status.py reconcile` (Phase 4 tooling) for post-repin verification once the human has acted.
Device UAT for either consumer's Gate-1 re-verification happens in that consumer's own project,
out of this hub phase's scope entirely.
</human_gated_boundary>

## Artifacts this phase produces

This plan produces no new source artifacts of its own — it verifies the final state of everything
`05-01-PLAN.md` and `05-02-PLAN.md` produced (ChipBar's `expandable`/`rawContent` params +
`ExpandableConfig`, `FilterBar.kt`'s removal, the new `SheetHeaderMenu` composable + its
`INTENTIONALLY_UNREGISTERED` entry, and the twice-rebaselined `api.txt`/`API.md`) and surfaces the
one artifact that is explicitly NOT produced autonomously by this phase: the `v2.0.0` git tag and
either consumer's repinned Gradle coordinate — both are human-executed, outside this plan's own
file scope, per the `<human_gated_boundary>` above.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full hub-side phase-gate verification</name>
  <files>(none — read-only verification of already-committed hub source)</files>
  <read_first>
    .planning/phases/05-gardening-unify-coordinated-repin/05-RESEARCH.md section "Validation
    Architecture" / "Sampling Rate" (the phase-gate command this task runs);
    .planning/ROADMAP.md section "Phase 5: Gardening — Unify & Coordinated Repin" (success criteria
    1-2, the scope this task confirms);
    docs/COHERENCE-AUDIT.md section "Unify Work-Order" (the 1:1 scope both prior plans implemented —
    re-check nothing beyond WO-1/WO-2 was touched, per the unify-scope lock).
  </read_first>
  <action>
    Run `./gradlew testDebugUnitTest detekt apiCheck` from the repo root — all three must succeed
    with zero failures and zero detekt findings (zero-baseline policy). Confirm
    `ComponentRegistry`'s registered-XOR-allowlisted invariant holds for the final post-unify state:
    the composable retired in WO-1 appears in neither `entries` nor `INTENTIONALLY_UNREGISTERED`
    (fully gone, not orphaned in either list); the composable extracted in WO-2 appears in
    `INTENTIONALLY_UNREGISTERED` only, never in `entries`; both `TextCardBottomSheet` and
    `ListCardBottomSheet` still each appear in `entries` exactly once, unchanged. Re-read
    `docs/COHERENCE-AUDIT.md`'s Unify Work-Order section once more and confirm both items are now
    fully implemented against the actual landed diff (`git log --oneline -10`) — nothing beyond
    those two items was touched, per the unify-scope lock (D-01). Confirm `API.md` and `api.txt`
    are internally consistent with the final registry state: no stale mention anywhere of the
    composable retired in WO-1, and the WO-2 extraction documented in both the registry allowlist
    and `API.md`'s sub-parts table.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest detekt apiCheck</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest detekt apiCheck` exits 0
    <!-- planner-discipline-allow: FilterBar -->
    - `grep -rc 'FilterBar' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistry.kt src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt | awk -F: '{s+=$2} END{print s}'` equals 0
    - `test ! -f src/main/java/io/github/ygaray/yahirandroidtaste/component/FilterBar.kt`
    - `grep -c '"SheetHeaderMenu" to' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistry.kt` equals 1
  </acceptance_criteria>
  <done>Both GARD-01 unify items (WO-1, WO-2) are verifiably complete; the hub's own build/test/lint/API gates are all green on `main`, ready for the human-gated tag cut.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>
  Cut git tag `v2.0.0` on hub `main` and begin the coordinated repin (SecondBrain single-hop
  `v1.10.0` -> `v2.0.0`; CalTracker two-hop `v1.5.0` -> `v1.10.0` catch-up -> `v2.0.0`, per D-05) —
  or hold.
  </decision>
  <context>
  All hub-side gates are green: `./gradlew testDebugUnitTest detekt apiCheck` pass against the
  rebaselined `api.txt`; the composable retired in WO-1 is gone; the composable extracted in WO-2 is
  wired into both sheets. Per CLAUDE.md's human-gated shipping rule and the sequential-in-hub
  convention, cutting the tag and repinning either consumer must NOT be done autonomously from this
  hub-scoped phase — this checkpoint exists to get your explicit go-ahead before that step.
  </context>
  <options>
    <option id="proceed">
      <name>Cut v2.0.0 now and begin the coordinated repin</name>
      <pros>Hub-side work is complete and verified; GARD-02 can move forward immediately per D-05's
      catch-up-then-gardening sequencing for CalTracker.</pros>
      <cons>Tags are immutable (CLAUDE.md) — this is a one-way step; SecondBrain and CalTracker each
      need their own Gate-1 re-verification pass after repinning, which happens outside this hub
      project entirely.</cons>
    </option>
    <option id="hold">
      <name>Hold — do not tag yet</name>
      <pros>More time to review the unified diff or coordinate consumer-side readiness before
      committing to an immutable tag.</pros>
      <cons>GARD-02 and ROADMAP Phase 5 success criteria 3-4 stay incomplete until this step
      runs.</cons>
    </option>
  </options>
  <resume-signal>
  Select: proceed or hold. If "proceed": follow `ECOSYSTEM.md` §7 +
  `~/.claude/context/workflows/repin.md` to cut the `v2.0.0` tag on hub `main`, then run each
  consumer's own repin channel — SecondBrain: single-hop to `v2.0.0`; CalTracker: catch-up hop to
  `v1.10.0` first (Gate-1-verified) per D-05, THEN the `v2.0.0` hop. Neither consumer's files are
  touched from this hub project. After both consumers report their own Gate-1 pass, run `python3
  ~/.claude/context/deps/repin_status.py reconcile --hub yahirandroidtaste` to prove both pins moved
  and update `ECOSYSTEM.md`'s repin-matrix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A (Task 1) | Read-only verification of already-committed hub source — no new trust boundary. |
| Hub main -> both consumers (Task 2) | The coordinated repin is the one point in this phase where a change crosses from the hub repo into two independent consumer applications. This is exactly the boundary CLAUDE.md's human-gated shipping rule protects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering / Spoofing | Git tag `v2.0.0` cut + consumer coordinate bump | high | mitigate | Never executed autonomously by this plan — gated behind the blocking `checkpoint:decision` above, requiring the human's explicit go-ahead; tags are immutable and consumers pin an immutable tag, never a moving ref, per CLAUDE.md's supply-chain-integrity invariant. |
| T-05-06 | Repudiation | Post-repin pin-state drift (a consumer silently ends up on the wrong tag) | medium | mitigate | `repin_status.py reconcile` (Phase 4 tooling) is the required post-repin step named in the checkpoint's own resume-signal — it proves both pins moved to the true, live tag state rather than trusting an unaudited hand report. |
| T-05-04 | (informational — pre-existing, not introduced here) | `verify-api-additive.sh` absolute-vs-relative path bug | medium | accept | Tracked in `.planning/STATE.md` Blockers/Concerns since before Phase 3; out of this phase's scope to fix. `./gradlew apiCheck`, which Task 1 re-runs, is a separate, working mechanism unaffected by that bug. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — this plan adds no new third-party dependency. |

</threat_model>

<verification>
Task 1's `./gradlew testDebugUnitTest detekt apiCheck` must be green before Task 2's checkpoint is
reached. Task 2 itself cannot be auto-approved (`workflow.auto_advance` does not apply to this
gate) — it requires the human's explicit "proceed" or "hold" selection.
</verification>

<success_criteria>
1. `testDebugUnitTest`, `detekt`, and `apiCheck` all pass clean on `main` with both WO-1 and WO-2
   landed.
2. The registered-XOR-allowlisted invariant holds for the final post-unify registry state.
3. The tag-cut + coordinated repin decision is explicitly surfaced to the human, with no autonomous
   execution of either step from this hub-scoped phase.
</success_criteria>

<output>
Create `.planning/phases/05-gardening-unify-coordinated-repin/05-03-SUMMARY.md` when done
</output>
