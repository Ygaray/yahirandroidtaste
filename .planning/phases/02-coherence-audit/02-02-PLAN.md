---
phase: 02-coherence-audit
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/COHERENCE-AUDIT.md
autonomous: true
requirements: [AUD-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A developer reading docs/COHERENCE-AUDIT.md sees the remaining 5 families (Buttons / FAB, Pickers, Feedback, Empty State, Progress / Metrics — 15 entries) fully enumerated by name + shipped tier, none re-derived."
    - "Every overlap/near-duplicate-sibling/altitude-mismatch finding raised in these 5 families carries an explicit unify/keep-with-rationale/prune disposition; families with genuinely no finding say so explicitly rather than being left blank."
    - "The Unify Work-Order section aggregates every 'unify' disposition from all 9 family sections (this plan's + 02-01's), each with its pre-computed SecondBrain + CalTracker_Android call-site counts — Phase 5 (Gardening) can read this section alone and get a complete, actionable list."
  artifacts:
    - "docs/COHERENCE-AUDIT.md (finalized)"
  key_links:
    - "COHERENCE-AUDIT.md Buttons-FAB/Pickers/Feedback/Empty-State/Progress-Metrics sections -> shipped Entry.tier values in the corresponding *FamilyScreen.kt files (must not contradict, per D-01)"
    - "COHERENCE-AUDIT.md 'unify' findings (all 9 sections) -> Unify Work-Order section (1:1 aggregation) -> Phase 5 Gardening work-order (downstream, out of this phase's scope)"
---

<objective>
Finish `docs/COHERENCE-AUDIT.md`: audit the 5 remaining families (Buttons / FAB, Pickers,
Feedback, Empty State, Progress / Metrics — 15 of 53 entries), then assemble the final "Unify
Work-Order" section aggregating every "unify" disposition raised across all 9 family sections
(02-01's four plus this plan's five), and re-run the phase's structural verification.

Purpose: completes AUD-01 — every registered family enumerated, every finding dispositioned, and
the concrete "unify" work-order Phase 5 (Gardening) executes against 1:1.
Output: `docs/COHERENCE-AUDIT.md` fully written, zero remaining placeholders, all 4 ROADMAP §Phase
2 success criteria satisfied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-coherence-audit/02-CONTEXT.md
@.planning/phases/02-coherence-audit/02-RESEARCH.md
@.planning/phases/02-coherence-audit/02-VALIDATION.md
@docs/DESIGN-INTENT.md

# Source files this plan reads (never re-derive tier — transcribe Entry.tier verbatim, D-01)
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt
@src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt

# This plan continues the same file 02-01-PLAN.md's tracer created (wave 2, sequenced after 02-01
# due to the shared-file dependency) — no SUMMARY re-read needed, the file itself is the state.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Buttons/FAB + Pickers + Feedback + Empty State families — enumerate, disposition, blast-radius</name>
  <files>docs/COHERENCE-AUDIT.md</files>
  <action>
Replace the `### Buttons / FAB` placeholder: transcribe all 3 entries' `name` + shipped `tier`
verbatim from `ButtonsFabFamilyScreen.kt`'s `buttonsFabFamilyEntries` list (`ExpandableFab`,
`CycleSubTypeButton`, `DynamicActionButton`). `Read` each component's actual public signature and
check for overlap/near-duplicate-sibling/altitude-mismatch candidates using the same D-03-litmus-
grounded method as the prior families — RESEARCH.md's own skim did not surface a seed here, but
that skim was explicitly non-exhaustive, so do the real check rather than assuming the family is
clean. State explicitly "No overlap/near-duplicate/altitude-mismatch findings for this family" if
that holds after reading, rather than leaving the section thin.

Replace the `### Pickers` placeholder: transcribe all 4 entries' `name` + shipped `tier` from
`PickersFamilyScreen.kt`'s `pickersFamilyEntries` list (`AccentColorPicker`, `IconPickerGrid`,
`CropOverlay`, `SegmentedOptionSelector`). Read each signature and apply the same check; state
explicitly if no finding.

Replace the `### Feedback` placeholder: transcribe all 3 entries' `name` + shipped `tier` from
`FeedbackFamilyScreen.kt`'s `feedbackFamilyEntries` list (`ConfirmationDialog`, `UndoCenterScreen`,
`AttentionCue`). This family is consumed by CalTracker_Android (`ConfirmationDialog`,
`ConfirmationDialogDefaults`) as well as SecondBrain — give it real attention, not a rubber stamp.
Apply the same check; state explicitly if no finding.

Replace the `### Empty State` placeholder: transcribe the single entry (`EmptyState`, PRIMITIVE)
from `EmptyStateFamilyScreen.kt`'s `emptyStateFamilyEntries` list. A 1-entry family cannot have an
intra-family overlap/near-duplicate finding by definition — state this explicitly rather than
inventing a cross-family comparison; only an altitude-mismatch check applies (re-confirm
`EmptyState`'s PRIMITIVE tier against the D-03 litmus only if something about its actual signature
looks domain-coupled — otherwise state the shipped tier stands).

For any "unify" disposition surfaced in any of these 4 families, run
`grep -rl "<ComponentName>" ~/Projects/SecondBrain/app/src ~/Projects/CalTracker_Android/app/src`
(read-only, per D-02) and record per-repo counts inline.
  </action>
  <verify>
    <automated>grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md | grep -qx 2 && grep -q "ExpandableFab" docs/COHERENCE-AUDIT.md && grep -q "ConfirmationDialog" docs/COHERENCE-AUDIT.md && grep -q "^### Empty State" docs/COHERENCE-AUDIT.md</automated>
  </verify>
  <done>Buttons / FAB (3), Pickers (4), Feedback (3), and Empty State (1) sections — 11 entries total — are fully enumerated with name+tier, every family's finding status is explicit (either a cited disposition or an explicit "no finding" statement), every "unify" disposition among them carries per-repo blast-radius counts, and the pending-placeholder count has dropped from 6 to 2.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Progress / Metrics family — enumerate, disposition, blast-radius (both-consumer attention)</name>
  <files>docs/COHERENCE-AUDIT.md</files>
  <action>
Replace the `### Progress / Metrics` placeholder: transcribe all 4 entries' `name` + shipped
`tier` verbatim from `ProgressFamilyScreen.kt`'s `progressFamilyEntries` list (`MetricBar`,
`ProgressRing`, `AnimatedStatValue`, `HeroStatCard`). Per RESEARCH.md's Pitfall 4, this family
(along with Feedback) is one of only two families both SecondBrain and CalTracker_Android draw
from — `AnimatedStatValue`, `HeroStatCard`, and `ProgressRing` are confirmed CalTracker imports
this session; `MetricBar` was not confirmed among CalTracker's imports, so verify it independently
rather than assuming it matches the other three's exposure. Read each component's actual public
signature and apply the same overlap/near-duplicate-sibling/altitude-mismatch check as prior
families, citing the D-03 litmus for any altitude candidate. State explicitly if no finding for an
entry.

For any "unify" disposition surfaced, run the D-02 blast-radius grep against BOTH consumer repos
(`grep -rl "<ComponentName>" ~/Projects/SecondBrain/app/src ~/Projects/CalTracker_Android/app/src`,
read-only) and record both repos' counts inline — do not report only a single-repo count for this
family, since Pitfall 4 flags it as needing both-consumer pre-flight specifically.
  </action>
  <verify>
    <automated>grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md | grep -qx 1 && grep -q "MetricBar" docs/COHERENCE-AUDIT.md && grep -q "ProgressRing" docs/COHERENCE-AUDIT.md && grep -q "AnimatedStatValue" docs/COHERENCE-AUDIT.md && grep -q "HeroStatCard" docs/COHERENCE-AUDIT.md</automated>
  </verify>
  <done>Progress / Metrics (4 entries) is fully enumerated with name+tier, every finding is dispositioned or explicitly stated absent, any "unify" disposition carries both SecondBrain and CalTracker_Android call-site counts, and the pending-placeholder count has dropped from 2 to 1 (only Unify Work-Order remains).</done>
</task>

<task type="auto">
  <name>Task 3: Assemble Unify Work-Order + final structural verification</name>
  <files>docs/COHERENCE-AUDIT.md</files>
  <action>
Replace the `### Unify Work-Order` placeholder. Read back through all 9 family sections now
written (Cards, Chips, Sheets, Buttons / FAB, Pickers, Feedback, Empty State, Progress / Metrics,
Tactile Foundation) and aggregate every "unify" disposition into this single section as a
checklist Phase 5 (Gardening) can execute against directly, without re-reading the rest of the
document. Each work-order item must state: the component(s) to unify, their family, a one-line
restatement of why (the overlap/near-duplicate-sibling shape), and the pre-computed SecondBrain +
CalTracker_Android call-site counts from that finding's section (D-02). If zero "unify"
dispositions were raised across all 9 families, state that explicitly ("No unify dispositions —
every flagged finding resolved keep-with-rationale or prune") rather than leaving the section
empty.

Then run the phase's structural verification (per `02-VALIDATION.md`) against the finished
document and fix any drift found before finishing:
1. `for f in Cards Chips Sheets "Buttons / FAB" Pickers Feedback "Empty State" "Progress / Metrics" "Tactile Foundation"; do grep -q "^### $f\$" docs/COHERENCE-AUDIT.md || echo "MISSING: $f"; done` — must print nothing.
2. `grep -c "unify\|keep-with-rationale\|prune" docs/COHERENCE-AUDIT.md` — cross-check the count is consistent with the number of findings actually raised across all 9 sections (every finding disposition uses one of these three exact words).
3. Re-read the finished Unify Work-Order section once more and confirm every entry listed there also appears as a "unify" disposition somewhere in the 9 family sections above it (no orphaned or invented work-order items) — this is the manual-only check `02-VALIDATION.md` documents (not fully automatable; confirm by direct read, note as such in the plan SUMMARY).
  </action>
  <verify>
    <automated>grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md | grep -qx 0 && for f in Cards Chips Sheets "Buttons / FAB" Pickers Feedback "Empty State" "Progress / Metrics" "Tactile Foundation" "Unify Work-Order"; do grep -q "^### $f\$" docs/COHERENCE-AUDIT.md || echo "MISSING: $f"; done</automated>
  </verify>
  <done>The "Unify Work-Order" section is non-placeholder and lists every "unify" disposition from all 9 family sections with per-repo blast-radius counts (or explicitly states there are none), the pending-placeholder count is 0, all 10 headings are still present, and a direct re-read confirms no work-order item lacks a corresponding "unify" finding in the body above it. `docs/COHERENCE-AUDIT.md` is complete and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan writes only `docs/COHERENCE-AUDIT.md` from existing, already-published repo source and read-only greps against two on-disk consumer trees. No runtime code path, network call, auth/session/crypto surface, or user input is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | `docs/COHERENCE-AUDIT.md` (blast-radius grep output + Unify Work-Order) | low | accept | The doc records file counts/paths from `~/Projects/SecondBrain` and `~/Projects/CalTracker_Android` — both are already-named, non-secret consumer repos referenced openly elsewhere in this hub's own `CLAUDE.md`/`ECOSYSTEM.md`; the Unify Work-Order itself is a documentation artifact (no code executes from it), consumed by a future human-gated phase (Phase 5), not automatically. |

No other STRIDE categories apply — this plan performs no writes outside `docs/COHERENCE-AUDIT.md`, installs no dependencies, and processes no external/untrusted input.
</threat_model>

<verification>
- `for f in Cards Chips Sheets "Buttons / FAB" Pickers Feedback "Empty State" "Progress / Metrics" "Tactile Foundation" "Unify Work-Order"; do grep -q "^### $f\$" docs/COHERENCE-AUDIT.md || echo "MISSING: $f"; done` prints nothing (AUD-01 success criterion 1).
- `grep -c "PENDING - filled by a later task" docs/COHERENCE-AUDIT.md` reads 2 after Task 1, 1 after Task 2, 0 after Task 3 (all 9 families + Unify Work-Order fully written).
- `grep -c "unify\|keep-with-rationale\|prune" docs/COHERENCE-AUDIT.md` cross-checked by direct read against the number of findings raised (AUD-01 success criteria 2-3).
- Direct read of the final "Unify Work-Order" section confirms it is a complete, self-contained, actionable list Phase 5 can execute against without re-reading the rest of the doc (AUD-01 success criterion 4 — manual-only per `02-VALIDATION.md`).
</verification>

<success_criteria>
- All 9 registered families are enumerated in `docs/COHERENCE-AUDIT.md` (ROADMAP §Phase 2 criterion 1).
- Every flagged overlap/near-duplicate-sibling/altitude-mismatch finding across all 9 families carries a documented unify/keep-with-rationale/prune disposition (criteria 2-3).
- The "Unify Work-Order" section is a concrete, actionable, self-contained list — every item traceable to a "unify" finding above it, every item carrying pre-computed consumer blast-radius counts — ready for Phase 5 (Gardening) to consume 1:1 (criterion 4).
- Zero `PENDING - filled by a later task` placeholders remain anywhere in the document.
</success_criteria>

<output>
Create `.planning/phases/02-coherence-audit/02-02-SUMMARY.md` when done
</output>
