---
phase: 01-tier-legibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DESIGN-INTENT.md
autonomous: true
requirements: [LEG-02]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DESIGN-INTENT.md's litmus (D-03: zero domain nouns + caller-content-only render = primitive; else = pattern) is a decidable binary test that assigns every borderline component (CardBase, ChipBar, HeatSwatch) to exactly one tier — the doc must show this test applied to all three named borderline cases, never leaving one ambiguous between both/neither tier."
    - "DESIGN-INTENT.md states the primitives contract and the patterns contract plus the litmus unconditionally — its content is not gated on the registry being empty or on any specific component existing."
    - "DESIGN-INTENT.md presents the primitives contract and the patterns contract as clearly labeled, independently-readable sections — the order they appear in the doc is not semantically significant to satisfying LEG-02."
  artifacts:
    - "docs/DESIGN-INTENT.md — new file, new top-level docs/ directory"
  key_links:
    - "docs/DESIGN-INTENT.md's litmus text -> the SAME two-question test applied in 01-01-PLAN.md/01-03-PLAN.md/01-04-PLAN.md's per-entry tier assignments (must not diverge)."
  prohibitions:
    - statement: "MUST NOT let docs/DESIGN-INTENT.md's worked-example tier claims (CardBase, ChipBar, HeatSwatch) diverge from ComponentRegistry's actual assigned tier for those same components — the doc and the code must agree."
      status: unresolved
    - statement: "MUST NOT name specific consumer apps (SecondBrain, CalTracker) or their domain concepts in docs/DESIGN-INTENT.md's litmus — the primitives/patterns contract must stay consumer-agnostic per the hub's one-way-dependency, no-domain-assumption invariant (CLAUDE.md \"The invariants\")."
      status: unresolved
---

<objective>
Write `docs/DESIGN-INTENT.md` — a design-intent doc, distinct from the registry-of-what-exists
(`ComponentRegistry.kt`) and the public-surface listing (`API.md`), that states what the hub means
to be per tier: the primitives contract, the patterns contract, and the decidable D-03 litmus,
applied to the three named borderline components (LEG-02).

Purpose: this is the phase's second, fully independent slice — no code dependency, runs in Wave 1
parallel to `01-01-PLAN.md`. Future phases (Phase 2's coherence audit, Phase 3's tier-aware
contribution litmus) read this doc as the canonical statement of intent, so it must state a
decidable test (not adjective-based judgment) and must not drift from what the code actually does.

Output: `docs/DESIGN-INTENT.md` (new file, new top-level directory).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-tier-legibility/01-CONTEXT.md
@.planning/phases/01-tier-legibility/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `ComponentRegistry.Tier` — new nested enum (`PRIMITIVE`, `PATTERN`) in `ComponentRegistry.kt`
  (lands in `01-01-PLAN.md`)
- `ComponentRegistry.Entry.tier` — new required field on `Entry` (lands in `01-01-PLAN.md`)
- `ComponentRow`'s new `tier: ComponentRegistry.Tier` parameter — `ExplorerIndexScreen.kt` (lands
  in `01-05-PLAN.md`)
- New `Badge` render block inside `ComponentDetailScreen`'s `TopAppBar` title (lands in
  `01-05-PLAN.md`)
- `docs/DESIGN-INTENT.md` — new file, new top-level `docs/` directory (THIS plan)
- `ComponentRegistryTierTest.kt` — new test file (lands in `01-05-PLAN.md`)
- `api.txt` — regenerated (lands in `01-05-PLAN.md`)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write docs/DESIGN-INTENT.md</name>
  <read_first>.planning/phases/01-tier-legibility/01-CONTEXT.md, CLAUDE.md</read_first>
  <files>docs/DESIGN-INTENT.md</files>
  <action>
  Create the new `docs/` directory and write `docs/DESIGN-INTENT.md` with EXACTLY this content
  (this is the full, final doc — write it verbatim, do not paraphrase or shorten):

  ---
  # Design Intent — yahirandroidtaste

  This document states what the hub *means to be*, per tier — distinct from `ComponentRegistry.kt`
  (the registry of what exists) and `API.md` (the public surface). Where those enumerate, this
  document sets intent: the contract each tier promises, and the litmus that decides which tier a
  component belongs to.

  Scope: this litmus applies to every component registered in `ComponentRegistry.entries`. Sub-parts
  listed in `ComponentRegistry.INTENTIONALLY_UNREGISTERED` are infrastructure exercised indirectly
  through their callers, not independently showcased — they sit outside the tier taxonomy entirely
  and are not tiered.

  ## The Primitives Contract

  A **primitive** is domain-agnostic presentation with zero opinions. It:
  - Introduces no domain vocabulary in its name or its parameters.
  - Renders only what the caller passes in — content, callbacks, and generic type parameters —
    never content it invents or assumes.
  - Holds no interaction convention, no composition opinion, no business rule of its own.

  A primitive is safe to drop into any consumer, in any domain, because it makes no assumptions
  about what it is showing.

  ## The Patterns Contract

  A **pattern** is an opinionated composition or interaction convention built from primitives (and
  possibly other patterns). It:
  - May introduce domain vocabulary in its name or parameters (e.g. a "card", a "tag").
  - Bakes in a specific interaction convention (e.g. reveal-confirm destructive swipe) or a
    specific composition of primitives that the hub has decided is the *right* shape for that
    convention.
  - Is still domain-agnostic in the sense that it names no *consumer's* concepts — it may say
    "card" or "tag" (the hub's own vocabulary), but never a specific consumer app's business
    objects.

  A pattern is still reusable across consumers, but it carries an opinion the caller does not have
  to re-derive.

  ## The Litmus

  The test is decidable, not adjective-based — "simpler" vs. "more opinionated" is exactly the
  kind of test that lets the same component be tiered two ways, which is why this litmus asks two
  yes/no questions instead:

  1. **Does the name or any parameter introduce a domain noun** (e.g. "Card", "Tag", "Voice",
     "Album")?
  2. **Does the component render only caller-passed content**, with no baked-in interaction
     convention (swipe-to-reveal, modal-chrome pattern, etc.) or composition opinion of its own?

  **PRIMITIVE** = "no" to (1) AND "yes" to (2).
  **PATTERN** = "yes" to (1) OR "no" to (2) — either condition alone is enough to make it a
  pattern.

  ## Worked Examples (the three borderline cases)

  - **`CardBase` -> PATTERN.** Its name carries no domain noun on its own, but it bakes in the
    hub's reveal-confirm destructive-swipe interaction convention (left = delete, right = edit)
    via the shared `SwipeableActionRow` infrastructure — an opinionated interaction convention, not
    caller-supplied content. Fails condition (2).
  - **`ChipBar` -> PRIMITIVE.** Fully generic (`fun &lt;T&gt; ChipBar(items: List&lt;T&gt;, key: (T) -&gt;
    Any, itemContent: @Composable (T) -&gt; Unit, ...)`), zero domain nouns in its name or
    parameters, and its own KDoc states it "holds no chip-rendering opinions and imports no
    `:app` type — it is pure presentation." Passes both conditions.
  - **`HeatSwatch` -> PATTERN.** Takes no caller-supplied content — it hardcodes its own sample
    data and renders a specific visual convention (the Heat relatedness ramp) targeting mindmap
    nodes/edges. Fails condition (2): it does not render only caller-passed content.

  ## Applying the Litmus

  When authoring a new component or tiering an existing one, ask both questions above in order. If
  either yes/no lands you in PATTERN, stop — you don't need to weigh the two conditions against
  each other, either one is sufficient. Read the component's actual public signature (not just how
  a caller happens to use it) before deciding — usage patterns can be a proxy but the true test is
  the signature and body, not the call site.
  ---

  Do not name any specific downstream consumer application anywhere in this file — the contract
  must stay consumer-agnostic, naming only the hub's own vocabulary ("card", "tag") never a
  specific consumer's business objects (this is prohibition #2 in this plan's must_haves).
  </action>
  <verify>
    <automated>test -f docs/DESIGN-INTENT.md && grep -c '## The Primitives Contract' docs/DESIGN-INTENT.md | grep -qx 1 && grep -c '## The Patterns Contract' docs/DESIGN-INTENT.md | grep -qx 1 && grep -c '## The Litmus' docs/DESIGN-INTENT.md | grep -qx 1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `test -f docs/DESIGN-INTENT.md` succeeds
  - `grep -c 'CardBase' docs/DESIGN-INTENT.md` returns >= 1 and the surrounding text states PATTERN
  - `grep -c 'ChipBar' docs/DESIGN-INTENT.md` returns >= 1 and the surrounding text states PRIMITIVE
  - `grep -c 'HeatSwatch' docs/DESIGN-INTENT.md` returns >= 1 and the surrounding text states PATTERN
  - `grep -c 'INTENTIONALLY_UNREGISTERED' docs/DESIGN-INTENT.md` returns >= 1 (the scope note)
  </acceptance_criteria>
  <done>docs/DESIGN-INTENT.md exists with the primitives contract, patterns contract, decidable litmus, and the three worked examples.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify content completeness and the consumer-agnostic prohibition</name>
  <read_first>docs/DESIGN-INTENT.md</read_first>
  <files>docs/DESIGN-INTENT.md</files>
  <action>
  Re-read the written doc and confirm every required element is present and no forbidden
  downstream-consumer application name leaked in. If this task's automated check below finds a
  match, remove the reference and rewrite that sentence to stay consumer-agnostic (name the hub's
  own vocabulary — "card", "tag" — never a specific consumer's business object). No code changes
  in this task, doc-only.
  </action>
  <verify>
    <automated>! grep -Eqi 'SecondBrain|CalTracker' docs/DESIGN-INTENT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -Eic 'SecondBrain|CalTracker' docs/DESIGN-INTENT.md` returns 0
  - `grep -c '## Worked Examples' docs/DESIGN-INTENT.md` returns 1
  - `grep -c '## Applying the Litmus' docs/DESIGN-INTENT.md` returns 1
  </acceptance_criteria>
  <done>docs/DESIGN-INTENT.md is complete, decidable, and consumer-agnostic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan writes a single static markdown documentation file. No user input, no network I/O, no code path, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | `docs/DESIGN-INTENT.md` | low | accept | Static markdown, no secrets, no dynamic content, committed to a public repo alongside existing public docs (`API.md`, `CLAUDE.md`). Nothing new to disclose. |

No package installs this phase (Package Legitimacy Gate not applicable, per RESEARCH.md).
</threat_model>

<verification>
- `docs/DESIGN-INTENT.md` exists and contains all 5 required sections (Primitives Contract,
  Patterns Contract, Litmus, Worked Examples, Applying the Litmus)
- CardBase/ChipBar/HeatSwatch worked examples state PATTERN/PRIMITIVE/PATTERN respectively
- No consumer app name (SecondBrain, CalTracker) appears anywhere in the file
</verification>

<success_criteria>
LEG-02 satisfied: a design-intent doc distinct from the registry states the primitives contract,
the patterns contract, and a decidable litmus with worked examples.
</success_criteria>

<output>
Create `.planning/phases/01-tier-legibility/01-02-SUMMARY.md` when done
</output>
