---
phase: 01-tier-legibility
plan: 05
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentDetailScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/CardsFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt
  - api.txt
  - src/test/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistryTierTest.kt
autonomous: true
requirements: [LEG-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Entry.tier is a required (no-default) enum field (D-01) — every one of the 53 registered entries has exactly one of PRIMITIVE/PATTERN at compile time; there is no null/missing-tier render state to design for."
    - "The label is a closed two-value enum (\"Primitive\" / \"Pattern\") rendered via Material3 Badge's own sizing — both strings are short, fixed, and never caller-supplied, so no truncation/wrap/overflow risk exists."
    - "Exactly one badge renders per Entry, always — the enum guarantees one-and-only-one tier value; there is no \"many badges\" or \"zero badges\" case to handle."
    - "The ExplorerActivity gallery displays each component's tier on both its list row (ComponentRow, reused by index search results and all 9 family screens' own row lists) and its detail-page header (ComponentDetailScreen's TopAppBar title) — a developer browsing the catalog can see tiering on both surfaces without reading source (D-02)."
    - "ComponentRegistry.entries.first { it.name == \"CardBase\" }.tier == PATTERN and .first { it.name == \"ChipBar\" }.tier == PRIMITIVE are asserted by an automated test, not just present in source — LEG-01's \"queryable in code\" criterion has a real, runnable proof."
    - "api.txt reflects the new Entry.tier field and ComponentRow's new tier parameter; ./gradlew apiCheck passes."
  artifacts:
    - "ExplorerIndexScreen.kt — ComponentRow gains a required tier param + a new internal TierBadge helper; its own search-results call site passes tier = entry.tier"
    - "ComponentDetailScreen.kt — TopAppBar title renders TierBadge(entry.tier) beside entry.name"
    - "All 9 *FamilyScreen.kt files' ComponentRow call sites pass tier = entry.tier"
    - "src/test/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistryTierTest.kt — new test asserting tier is genuinely queryable"
    - "api.txt — regenerated, committed alongside the code in the same commit"
  key_links:
    - "Entry.tier (data, landed across 01-01/01-03/01-04) -> ComponentRow's new tier param -> shared internal TierBadge helper -> Badge render (list surface)"
    - "Entry.tier -> ComponentDetailScreen's TierBadge(entry.tier) call -> Badge render (detail surface) — same shared helper, so label/color logic lives in exactly one place and cannot drift between the two surfaces"
    - "Entry ctor signature change (01-01) + ComponentRow signature change (this plan) -> single apiDump pass -> api.txt -> apiCheck green"
---

<objective>
Wire `Entry.tier` into both gallery surfaces — `ComponentRow` (list/search, reused by all 9 family
screens) and `ComponentDetailScreen` (detail header) — via a shared internal `TierBadge` helper,
then run the deferred full-project verification: `./gradlew compileDebugKotlin`, `apiDump`/
`apiCheck`, `detekt`, and the full test suite, including a new test proving `tier` is genuinely
queryable in code (LEG-01).

Purpose: this is the first plan in the phase that can compile the whole project — it depends on
BOTH `01-03-PLAN.md` (Sheets) and `01-04-PLAN.md` (the remaining 6 families), which together with
`01-01-PLAN.md` (Cards + Chips) complete all 53 `Entry.tier` assignments. Wave 3 runs strictly
after Wave 2 merges, so this is the first point the required-no-default field's full-module
compile-completeness constraint is satisfied. This plan therefore does double duty: it is the
phase's UI-layer expansion (D-02's gallery-display criterion) AND the phase's first real
integration checkpoint (full compile + apiCheck + test suite green).

Output: tier badge visible on both gallery surfaces; api.txt rebaselined; a new automated test
proves LEG-01's "queryable in code" criterion; full test suite + detekt green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-tier-legibility/01-CONTEXT.md
@.planning/phases/01-tier-legibility/01-RESEARCH.md
@.planning/phases/01-tier-legibility/01-PATTERNS.md
@.planning/phases/01-tier-legibility/01-UI-SPEC.md
</context>

## Artifacts this phase produces

- `ComponentRegistry.Tier` — new nested enum (`PRIMITIVE`, `PATTERN`) in `ComponentRegistry.kt`
  (landed in `01-01-PLAN.md`)
- `ComponentRegistry.Entry.tier` — new required field on `Entry` (landed in `01-01-PLAN.md`)
- `ComponentRow`'s new `tier: ComponentRegistry.Tier` parameter + new internal `TierBadge` helper
  — `ExplorerIndexScreen.kt` (THIS plan)
- New `TierBadge(entry.tier)` call inside `ComponentDetailScreen`'s `TopAppBar` title (THIS plan;
  no new composable name in this file — reuses the shared `TierBadge` from
  `ExplorerIndexScreen.kt`, same package)
- `docs/DESIGN-INTENT.md` — new file (landed in `01-02-PLAN.md`)
- `ComponentRegistryTierTest.kt` — new test file (THIS plan)
- `api.txt` — regenerated (THIS plan)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add tier param + shared TierBadge helper to ComponentRow</name>
  <precondition>All 53 ComponentRegistry.Entry(...) call sites carry tier = ... (landed by 01-01-PLAN.md, 01-03-PLAN.md, and 01-04-PLAN.md — Waves 1-2 complete)</precondition>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt, .planning/phases/01-tier-legibility/01-UI-SPEC.md</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt</files>
  <action>
  Add these imports to ExplorerIndexScreen.kt: `androidx.compose.foundation.layout.Row`,
  `androidx.compose.foundation.layout.Spacer`, `androidx.compose.foundation.layout.width`,
  `androidx.compose.material3.Badge`, `androidx.compose.ui.Alignment` (`dp` is already imported).

  Add a new internal helper, placed just above `fun ComponentRow(...)`:

  `@Composable internal fun TierBadge(tier: ComponentRegistry.Tier)` — renders a Material3
  `Badge` whose `containerColor`/`contentColor` come from a `when (tier)` mapping
  `PRIMITIVE -> MaterialTheme.colorScheme.secondaryContainer to onSecondaryContainer`,
  `PATTERN -> MaterialTheme.colorScheme.tertiaryContainer to onTertiaryContainer` (per
  01-UI-SPEC.md's Color contract — do NOT use `colorScheme.primary`, reserved for
  `SectionLabel`/FAB/interactive emphasis), with `Text("Primitive")` for PRIMITIVE and
  `Text("Pattern")` for PATTERN inside the `Badge`'s content slot (exact strings, title case, per
  01-UI-SPEC.md's Copywriting Contract). This is an internal, non-public helper (not a new
  showcaseable component, mirrors the existing `internal fun SectionLabel`/`internal fun
  DividerRow` precedent in ComponentDetailScreen.kt) — do NOT register it in ComponentRegistry
  and do NOT add it to `component/`. Placing it here (not ComponentDetailScreen.kt) means BOTH
  gallery surfaces call the SAME function, so the label/color mapping lives in exactly one place
  and cannot drift between the list and detail surfaces.

  Change `ComponentRow`'s signature from
  `fun ComponentRow(name: String, supportingLabel: String? = null, onClick: () -> Unit)` to
  `fun ComponentRow(name: String, tier: ComponentRegistry.Tier, supportingLabel: String? = null,
  onClick: () -> Unit)` — `tier` is required, no default (every one of the 10 call sites has a
  real `entry.tier` to pass; a default would let a caller silently omit the badge).

  Change `ComponentRow`'s `headlineContent` from the bare `{ Text(name) }` to:
  `{ Row(verticalAlignment = Alignment.CenterVertically) { Text(name); Spacer(Modifier.width(8.dp));
  TierBadge(tier) } }`. Do NOT touch `leadingContent` (stays unused/reserved) or the existing
  `trailingContent` chevron. The badge is non-interactive — do NOT wrap it in
  `Modifier.clickable`; the row's own `onClick` still fires from anywhere on the row.

  Update THIS file's own `ComponentRow` call site (the index screen's search-results loop, inside
  `results.forEach { entry -> ComponentRow(name = entry.name, supportingLabel = ..., onClick =
  ...) }`) to add `tier = entry.tier,`.
  </action>
  <verify>
    <automated>grep -c 'internal fun TierBadge' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt | grep -qx 1 && grep -c 'fun ComponentRow(name: String, tier: ComponentRegistry.Tier' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt | grep -qx 1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'internal fun TierBadge(tier: ComponentRegistry.Tier)' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt` returns 1
  - `grep -c 'fun ComponentRow(name: String, tier: ComponentRegistry.Tier' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt` returns 1
  - `grep -c 'tier = entry.tier' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt` returns 1 (this file's own search-results call site)
  - `grep -c '"Primitive"' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt` returns 1 and `grep -c '"Pattern"' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ExplorerIndexScreen.kt` returns 1
  </acceptance_criteria>
  <done>ComponentRow requires a tier param and renders a TierBadge; the shared helper exists; this file's own call site is updated. Full project compile is NOT expected to pass yet — the 9 family screens' ComponentRow call sites still lack tier= (Task 2 completes the sweep).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sweep the 9 family screens' ComponentRow call sites + wire ComponentDetailScreen's badge — first full green compile</name>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentDetailScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/CardsFamilyScreen.kt</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentDetailScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/CardsFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ChipsFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/SheetsFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt</files>
  <action>
  In each of the 9 `*FamilyScreen.kt` files, find the single `ComponentRow(name = entry.name,
  onClick = { onNavigateToDetail(entry.name) })` call site (each family screen's own row-list
  `items(...) { entry -> ComponentRow(...) }` block) and add `tier = entry.tier,` as a new named
  argument: `ComponentRow(name = entry.name, tier = entry.tier, onClick = { onNavigateToDetail(entry.name) })`.
  This is a single, identical one-line edit in all 9 files (mechanical — `ComponentRow` is now a
  required-tier-param composable, so every caller must supply it, matching Task 1's Entry.tier
  precedent).

  In ComponentDetailScreen.kt, change the `TopAppBar`'s `title` from
  `{ Text(entry.name, fontWeight = FontWeight.Medium) }` to:
  `{ Row(verticalAlignment = Alignment.CenterVertically) { Text(entry.name, fontWeight =
  FontWeight.Medium); Spacer(Modifier.width(8.dp)); TierBadge(entry.tier) } }`. `entry` is already
  in scope (`fun ComponentDetailScreen(entry: ComponentRegistry.Entry, ...)`) — no new parameter
  needed. `Row`, `Spacer`, and `Alignment` are already imported in this file; `TierBadge` needs no
  import — it is `internal` in the same `io.github.ygaray.yahirandroidtaste.explorer` package.

  Once every file in this task is edited, run `./gradlew compileDebugKotlin` — this is the FIRST
  point in this phase the whole module compiles (all 53 Entry.tier sites + both ComponentRow
  signature-change call sites now exist together). If it fails, the error names the exact missing
  call site — fix and re-run before proceeding.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin --console=plain 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
  - `./gradlew compileDebugKotlin` exits 0
  - `grep -c 'tier = entry.tier' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/CardsFamilyScreen.kt` returns 1, and the same for the other 8 `*FamilyScreen.kt` files (9 total, one per file)
  - `grep -c 'TierBadge(entry.tier)' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentDetailScreen.kt` returns 1
  - `./gradlew testDebugUnitTest --tests "io.github.ygaray.yahirandroidtaste.explorer.*" --console=plain` exits 0 (ComponentRegistrySearchTest, ComponentRegistryDriftGuardTest, GalleryDemoInteractionTest, etc. — none construct a raw `Entry(...)` or `ComponentRow(...)`, so no test-code edits are expected)
  </acceptance_criteria>
  <done>All 9 family screens pass tier = entry.tier to ComponentRow; ComponentDetailScreen renders TierBadge(entry.tier) in its TopAppBar title; the whole module compiles for the first time this phase; the explorer package's existing unit tests stay green with no test-code edits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rebaseline api.txt, run detekt, add the tier-queryability test</name>
  <read_first>src/test/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistrySearchTest.kt</read_first>
  <files>api.txt, src/test/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistryTierTest.kt</files>
  <action>
  Run `./gradlew apiDump` — this regenerates `api.txt` to reflect BOTH public-signature changes
  this phase makes: `Entry`'s new `tier: Tier` constructor parameter (from 01-01-PLAN.md) and
  `ComponentRow`'s new `tier: ComponentRegistry.Tier` parameter (from Task 1 above), plus the new
  `Tier` enum's own `enum_constant` entries. Review the diff — it should show exactly these
  additions, nothing else. Commit the regenerated `api.txt` in the same commit as this task's other
  changes (per this project's existing convention — do not hand-edit `api.txt`).

  Create `src/test/java/io/github/ygaray/yahirandroidtaste/explorer/ComponentRegistryTierTest.kt`,
  mirroring `ComponentRegistrySearchTest.kt`'s plain-JUnit4, no-Robolectric,
  vacuous-pass-guard-first style. Package `io.github.ygaray.yahirandroidtaste.explorer`, imports
  `org.junit.Assert.assertEquals`, `org.junit.Assert.assertTrue`, `org.junit.Test`. Include these
  test methods:
  - `registryIsNotEmpty_vacuousPassGuard()` — asserts `ComponentRegistry.entries.isNotEmpty()`
    with a message identical in spirit to `ComponentRegistrySearchTest`'s own guard.
  - `cardBase_isTieredPattern()` — `ComponentRegistry.entries.first { it.name == "CardBase" }.tier`
    equals `ComponentRegistry.Tier.PATTERN`.
  - `chipBar_isTieredPrimitive()` — `ComponentRegistry.entries.first { it.name == "ChipBar" }.tier`
    equals `ComponentRegistry.Tier.PRIMITIVE`.
  - `heatSwatch_isTieredPattern()` — `ComponentRegistry.entries.first { it.name == "HeatSwatch" }.tier`
    equals `ComponentRegistry.Tier.PATTERN`.

  These three assertions are a genuine, runnable cross-check that docs/DESIGN-INTENT.md's three
  worked examples (CardBase/ChipBar/HeatSwatch) match the actual registry values — if this test
  passes, that consistency is proven, not merely claimed.

  Run `./gradlew apiCheck` (must pass — confirms api.txt now matches the compiled signatures),
  `./gradlew detekt` (must pass — `LongParameterList` stays under threshold per RESEARCH.md
  Pitfall 4, no suppression needed), and the full `./gradlew testDebugUnitTest` (must pass,
  including the new `ComponentRegistryTierTest`).
  </action>
  <verify>
    <automated>./gradlew apiDump apiCheck detekt testDebugUnitTest --console=plain 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
  - `./gradlew apiCheck` exits 0
  - `./gradlew detekt` exits 0
  - `./gradlew testDebugUnitTest --tests "io.github.ygaray.yahirandroidtaste.explorer.ComponentRegistryTierTest"` exits 0 with 4 tests passing
  - `git diff --stat api.txt` shows changes limited to `Entry`'s ctor line, the new `Tier` enum's `enum_constant` lines, and `ComponentRow`'s signature line — no unrelated public-API drift
  </acceptance_criteria>
  <done>api.txt is rebaselined and committed; apiCheck/detekt/full test suite are all green; ComponentRegistryTierTest proves LEG-01's "queryable in code" criterion and cross-checks the 3 DESIGN-INTENT.md worked examples against the real registry values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan renders a non-interactive text badge on a debug-only standalone gallery harness (`ExplorerActivity`, not shipped consumer UI) and regenerates a build-time API baseline file. No user input, no network I/O, no auth/session, no persisted/serialized data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Tampering | `TierBadge` render (`ComponentRow`/`ComponentDetailScreen`) | low | accept | The badge's `Text` content is always one of exactly two fixed literals ("Primitive"/"Pattern") derived from a closed enum via a fixed `when` — never a free-form/user-supplied string, so there is no injection or spoofing surface. |
| T-01-05-02 | Information Disclosure | `api.txt` (regenerated) | low | accept | `api.txt` is an existing, already-public, already-committed build artifact documenting the library's own public surface — regenerating it discloses nothing new beyond the intentional `Entry`/`ComponentRow`/`Tier` additions this phase makes. |

No package installs this phase (Package Legitimacy Gate not applicable, per RESEARCH.md).
</threat_model>

<verification>
- `./gradlew compileDebugKotlin` succeeds (first full green compile this phase)
- `./gradlew testDebugUnitTest` succeeds, including the new `ComponentRegistryTierTest` (4 tests)
- `./gradlew apiCheck` succeeds against the regenerated `api.txt`
- `./gradlew detekt` succeeds (zero-baseline policy maintained, no new suppression)
- Both gallery surfaces (`ComponentRow` list rows, `ComponentDetailScreen` header) render a
  `TierBadge` via the SAME shared internal helper — no duplicated/divergent color-or-label logic
</verification>

<success_criteria>
LEG-01 fully satisfied: every registry entry's tier is queryable in code (proven by
`ComponentRegistryTierTest`) and visibly shown on both gallery surfaces (D-02). `api.txt` matches
the compiled public surface; the full test suite, `apiCheck`, and `detekt` are all green.
</success_criteria>

<output>
Create `.planning/phases/01-tier-legibility/01-05-SUMMARY.md` when done
</output>
