---
phase: 01-tier-legibility
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt
  - src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt
autonomous: true
requirements: [LEG-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 19 remaining registry entries (ButtonsFab 3, Pickers 4, Feedback 3, EmptyState 1, Progress 4, TactileFoundation 4) carry an explicit tier value applying the D-03 litmus."
    - "The TactileFoundation family (ElevationLadder, TactileTypeShowcase, GradientSwatch, HeatSwatch) is tiered entirely PATTERN — this family exists specifically to showcase the hub's own tactile-design tokens/visual language, so every entry in it is, by definition, one of the hub's own opinions made visible."
    - "Once this plan and 01-03-PLAN.md have both landed, all 53 of 53 registry entries carry an explicit tier value — the registry-wide tiering sweep is complete and the module is ready to compile (proven in 01-05-PLAN.md, Wave 3)."
  artifacts:
    - "ButtonsFabFamilyScreen.kt — all 3 Entry(...) call sites carry tier = ..."
    - "EmptyStateFamilyScreen.kt — the 1 Entry(...) call site carries tier = ..."
    - "PickersFamilyScreen.kt — all 4 Entry(...) call sites carry tier = ..."
    - "FeedbackFamilyScreen.kt — all 3 Entry(...) call sites carry tier = ..."
    - "ProgressFamilyScreen.kt — all 4 Entry(...) call sites carry tier = ..."
    - "TactileFoundationFamilyScreen.kt — all 4 Entry(...) call sites carry tier = ..."
  key_links:
    - "These 6 files' 19 tiered entries + 01-03-PLAN.md's 18 Sheets entries + 01-01-PLAN.md's 16 Cards/Chips entries = all 53 -> ComponentRegistry.entries compiles cleanly for the first time in 01-05-PLAN.md (Wave 3)."
---

<objective>
Apply the D-03 litmus to tier the remaining 19 registry entries across 6 family-screen files
(ButtonsFab 3, Pickers 4, Feedback 3, EmptyState 1, Progress 4, TactileFoundation 4) — the final
slice of the registry-wide tiering sweep started in `01-01-PLAN.md`.

Purpose: this plan, together with `01-03-PLAN.md` (Sheets), completes ALL 53 `Entry.tier`
assignments. It depends on `01-01-PLAN.md` (Wave 1) for the `ComponentRegistry.Tier` enum to
exist, and runs in Wave 2 in parallel with `01-03-PLAN.md` — the two plans touch disjoint files, so
they execute concurrently, but NEITHER reaches a full green project compile on its own (each is
missing the other's sites). That full compile is deferred to `01-05-PLAN.md` (Wave 3), the first
point after both merge — a hard compiler constraint of a required, no-default field on a class
instantiated across the whole module, not an oversight. This plan's own verification is therefore
source-level (grep-confirmed per task).

Output: 19 of 53 registry entries (the final 6 families) tiered. Combined with 01-01-PLAN.md (16)
and 01-03-PLAN.md (18), all 53 registry entries are tiered once this plan completes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-tier-legibility/01-CONTEXT.md
@.planning/phases/01-tier-legibility/01-RESEARCH.md
@.planning/phases/01-tier-legibility/01-01-PLAN.md
</context>

## Artifacts this phase produces

- `ComponentRegistry.Tier` — new nested enum (`PRIMITIVE`, `PATTERN`) in `ComponentRegistry.kt`
  (landed in `01-01-PLAN.md`)
- `ComponentRegistry.Entry.tier` — new required field on `Entry` (landed in `01-01-PLAN.md`)
- `ComponentRow`'s new `tier: ComponentRegistry.Tier` parameter — `ExplorerIndexScreen.kt` (lands
  in `01-05-PLAN.md`)
- New `Badge` render block inside `ComponentDetailScreen`'s `TopAppBar` title (lands in
  `01-05-PLAN.md`)
- `docs/DESIGN-INTENT.md` — new file (landed in `01-02-PLAN.md`)
- `ComponentRegistryTierTest.kt` — new test file (lands in `01-05-PLAN.md`)
- `api.txt` — regenerated (lands in `01-05-PLAN.md`)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tier ButtonsFab (3) + EmptyState (1)</name>
  <precondition>ComponentRegistry.Tier enum exists in ComponentRegistry.kt (landed by 01-01-PLAN.md, Wave 1)</precondition>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt</files>
  <action>
  Add `tier = ComponentRegistry.Tier.X` as the trailing named argument to each `ComponentRegistry.Entry(...)`
  call site, applying the D-03 litmus:

  In ButtonsFabFamilyScreen.kt (3 entries):
  - ExpandableFab -> PATTERN (its own params bake in domain nouns: `onCreateTextCard`,
    `onCreateListCard`, `onCreateVoiceCard`, `onAlbumCamera`, `onAlbumGallery`, `onNewTag` —
    fails condition (1) heavily)
  - CycleSubTypeButton -> PATTERN (bakes in a fixed 3-way icon-cycling convention keyed off
    `currentSubType: String` — a composition opinion, fails condition (2))
  - DynamicActionButton -> PATTERN (its `role: ActionButtonDefaults.ActionButtonRole` selects a
    specific Button-vs-TextButton composition + color scheme — a composition opinion, fails
    condition (2))

  In EmptyStateFamilyScreen.kt (1 entry):
  - EmptyState -> PRIMITIVE (`EmptyState(icon, title, modifier, body, ctaLabel, onCta)` — zero
    domain nouns, renders only caller-passed content; already reused generically elsewhere in this
    same codebase, e.g. ExplorerIndexScreen's own "No components found" state)

  Use fully-qualified `ComponentRegistry.Tier.X` in both files.
  </action>
  <verify>
    <automated>C1=$(grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'); C2=$(grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'); [ "$C1" = "3" ] && [ "$C2" = "1" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ButtonsFabFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.PATTERN'` returns 3
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/EmptyStateFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.PRIMITIVE'` returns 1
  </acceptance_criteria>
  <done>All 3 ButtonsFab entries and the 1 EmptyState entry carry an explicit tier value.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tier Pickers (4) + Feedback (3)</name>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt</files>
  <action>
  Add `tier = ComponentRegistry.Tier.X` as the trailing named argument to each entry, applying the
  D-03 litmus:

  In PickersFamilyScreen.kt (4 entries):
  - AccentColorPicker -> PATTERN (renders the hub's own predefined accent-color palette, not
    caller-passed content — fails condition (2))
  - IconPickerGrid -> PATTERN (renders the hub's own internal icon catalog, not caller-passed
    content — fails condition (2))
  - CropOverlay -> PATTERN (bakes in a specific crop-rectangle drag/resize interaction convention
    and renders no caller content at all — fails condition (2); this is the closest call in this
    plan, double-check against `component/CropOverlay.kt`'s actual signature before committing if
    unsure)
  - SegmentedOptionSelector -> PRIMITIVE (`SegmentedOptionSelector(selectedIndex, options:
    List&lt;String&gt;, onSelect, modifier, enabled, disabledReason)` — zero domain nouns, renders
    caller-passed option labels, no baked-in domain convention)

  In FeedbackFamilyScreen.kt (3 entries):
  - ConfirmationDialog -> PRIMITIVE (`ConfirmationDialog(title, body, onDismissRequest, onConfirm,
    modifier, confirmLabel, dismissLabel, confirmStyle, properties)` — zero domain nouns, renders
    only caller-passed title/body/labels, generic confirm/cancel shell)
  - UndoCenterScreen -> PATTERN (a fully composed screen with its own hardcoded "Undo History"
    title and `UndoHistoryEntry` domain model — the hub's own standardized undo-feedback
    convention, not a primitive)
  - AttentionCue -> PRIMITIVE (`AttentionCue(text, modifier, style, icon, tint,
    contentDescription)` — zero domain nouns, renders caller-passed text/icon, generic hint/cue
    styles)

  Use fully-qualified `ComponentRegistry.Tier.X` in both files.
  </action>
  <verify>
    <automated>C1=$(grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'); C2=$(grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'); [ "$C1" = "4" ] && [ "$C2" = "3" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/PickersFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.PRIMITIVE'` returns 1 (SegmentedOptionSelector only)
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/FeedbackFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.PRIMITIVE'` returns 2 (ConfirmationDialog, AttentionCue)
  </acceptance_criteria>
  <done>All 4 Pickers entries and all 3 Feedback entries carry an explicit tier value.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tier Progress (4) + TactileFoundation (4) — completes all 53 registry entries</name>
  <read_first>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt</read_first>
  <files>src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt, src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt</files>
  <action>
  Add `tier = ComponentRegistry.Tier.X` as the trailing named argument to each entry, applying the
  D-03 litmus — this completes the final 8 of 53 registry entries:

  In ProgressFamilyScreen.kt (4 entries):
  - MetricBar -> PATTERN (a specific labeled-progress-bar-with-band-coloring composition tied to
    `MetricBand` and the hub's `MaterialTheme.expressive.*` theme extension — a composition
    opinion, not neutral pass-through)
  - ProgressRing -> PATTERN (accepts a `content` slot but wraps it in a specific animated ring
    composition with its own default `MaterialTheme.expressive` motion/color opinion)
  - AnimatedStatValue -> PRIMITIVE (`AnimatedStatValue(targetValue: Float, modifier, style, color,
    animationSpec, format: (Float) -&gt; String)` — purely mechanical float-interpolation +
    formatting utility, zero domain nouns, no baked-in domain assumption)
  - HeroStatCard -> PATTERN (a specific "hero card" layout convention: label+value+accent-gradient
    stripe+optional content, tied to `MaterialTheme.expressive.heroGradient` — a composition
    opinion)

  In TactileFoundationFamilyScreen.kt (4 entries) — this whole family exists to showcase the hub's
  OWN tactile-design tokens, so every entry is PATTERN by definition (each renders the hub's own
  fixed demo, not caller-passed content):
  - ElevationLadder -> PATTERN (`ElevationLadder(modifier)` — no content param, renders its own
    hardcoded elevation-token showcase)
  - TactileTypeShowcase -> PATTERN (`TactileTypeShowcase(modifier)` — no content param, renders
    its own hardcoded type-scale showcase)
  - GradientSwatch -> PATTERN (`GradientSwatch(accentColor: Color, modifier)` — renders the hub's
    own specific gradient-swatch visual convention, not caller-passed content)
  - HeatSwatch -> PATTERN (the RESEARCH.md/CONTEXT.md locked worked example — hardcodes its own
    sample data and renders the Heat relatedness ramp convention)

  Use fully-qualified `ComponentRegistry.Tier.X` in both files.
  </action>
  <verify>
    <automated>C1=$(grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'); C2=$(grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.'); [ "$C1" = "4" ] && [ "$C2" = "4" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/ProgressFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.PRIMITIVE'` returns 1 (AnimatedStatValue only)
  - `grep -v '^\s*//' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt | grep -c 'tier = ComponentRegistry\.Tier\.PATTERN'` returns 4 (all 4)
  - `grep -A2 'name = "HeatSwatch"' src/main/java/io/github/ygaray/yahirandroidtaste/explorer/TactileFoundationFamilyScreen.kt` region contains `Tier.PATTERN` — spot-check the locked worked example
  </acceptance_criteria>
  <done>All 4 Progress entries and all 4 TactileFoundation entries carry an explicit tier value. All 53 of 53 registry entries are now tiered (16 from 01-01-PLAN.md + 18 from 01-03-PLAN.md + 19 from this plan) — the registry-wide sweep is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan populates a compile-time enum field at 19 source-code call sites across 6 files. No user input, no network I/O, no auth/session, no persisted/serialized data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Tampering | 6 `*FamilyScreen.kt` `Entry.tier` assignments | low | accept | `Tier` is a closed 2-value Kotlin enum, assigned only by source code under repo-owner control at compile time — no runtime/user-supplied string is parsed into it. |

No package installs this phase (Package Legitimacy Gate not applicable, per RESEARCH.md).
</threat_model>

<verification>
- All 19 remaining `ComponentRegistry.Entry(...)` call sites across the 6 files carry
  `tier = ComponentRegistry.Tier.X`
- 4 of the 19 are PRIMITIVE (EmptyState, SegmentedOptionSelector, ConfirmationDialog,
  AttentionCue, AnimatedStatValue — 5 actually, see per-task acceptance criteria for the exact
  per-file counts); the rest are PATTERN
- HeatSwatch (the locked worked example) is PATTERN
- Combined with 01-01-PLAN.md and 01-03-PLAN.md, all 53 of 53 registry entries carry an explicit
  tier value once this plan completes — full project compile is proven in 01-05-PLAN.md (Wave 3)
</verification>

<success_criteria>
All 19 remaining registry entries carry a defensible tier value per the D-03 litmus; all 53 of 53
registry entries are tiered once this plan completes.
</success_criteria>

<output>
Create `.planning/phases/01-tier-legibility/01-04-SUMMARY.md` when done
</output>
