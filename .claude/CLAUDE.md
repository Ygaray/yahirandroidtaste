# yahirandroidtaste — GSD project pointer

GSD-managed project. **The authoritative guide is the root `CLAUDE.md`** (repo root) — charter,
reusability invariants, toolchain, conventions. Read that; it is the single source of truth.

Planning artifacts live in `.planning/` (`PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`).

GSD workflow guidance loads just-in-time when a `/gsd-*` command runs — intentionally not
duplicated here (see `INC-2026-08-28-04`).
